# 开发记录 - 洪荒之后 (AfterHongHuang)

---

## 2026-07-16 | BUG-20260716-001 战后继续地图空白页自动化回归通过

### 问题与处理
- [x] 用户窗口反馈：战斗胜利后点击“继续”进入只剩 TopBar 的空白 `MapScene`。
- [x] 根因确认：继续按钮此前承担胜利最终结算并走 `OpenMapOnEnter` + deferred 地图展开，导致可见中间态。
- [x] 开发部 C10：敌人死亡即时锁定 `BattleOver/PlayerWon`；奖励领取/明确跳过独立结算；继续只负责导航；战后直接写 `MapEntryMode.OpenInteractiveMap`；地图构建失败显示明确错误页。
- [x] QA质量保证部以 `gpt-5.6-sol` / `max` 严格只读回归通过。Debug/Release 构建、Godot 4.7 Mono headless、Battle/Map 场景解析均通过，未发现 P0/P1。
- [ ] 仍需用户窗口复测，完成前任务保持 `Review`。

### 残留风险
- QA 记录 P2：缺 RewardProfile、候选不足和明确跳过端口尚无单独动态自检；生产保护已静态确认，不阻断本轮窗口复测。

---

## 2026-07-16 | BUG-20260716-002 继续行为纠正为胜利页地图 overlay

### 收口结论
- [x] 撤销“继续离开 Battle 并进入 MapScene”的错误实现口径。
- [x] Continue 现在只打开 Battle 胜利/奖励页上的互动地图 overlay；关闭 overlay 不重建、不隐藏、不清理原胜利/奖励 UI。
- [x] 敌人死亡即时完成 `Completed` 结果；奖励独立可选；只有地图合法节点点击才清理 Battle 并进入目标节点。
- [x] QA质量保证部以 Sol/max 严格只读回归：无 P0，Debug/Release build、Godot headless、500 seeds、Battle/Map 场景解析通过。
- [ ] 用户窗口待测：奖励未领取/部分领取/错误及 3 选 2 中途状态下的地图开关、关闭恢复、重复打开和合法节点点击。

### 残留风险
- R-059：目标节点激活失败时，当前离场链尚不能回滚原胜利页；当前生产图 500 seeds 未命中该数据，不阻断窗口实测。

---

## 2026-07-16 | ACT1 C7-C9 独立自动化门禁通过，转入窗口验收

### 收口结论
- [x] 开发部完成 C7-C9：奖励计划/候选防伪、多选领取、胜利与离场原子回滚、永久牌组不可变边界、敌方意图完整身份快照、来源数据驱动 `MergeRule`。
- [x] QA质量保证部以 `gpt-5.6-sol` / `max` 严格只读回归通过；未发现 P0/P2。
- [x] Debug/Release build 0 错误 0 警告；Godot 4.7 Mono headless 自检通过；500 seeds 无失败和交叉；Map、Battle、Lingmai、Shop、Event、DaoMarkSelect 场景解析通过；Release 无 Debug 胜利入口。
- [ ] 进入用户窗口实测，当前仅可标记 Review，不提前标 Done。

### 残留风险
- R-039 保持 P1：`RewardId` 消费记录未持久化，道痕候选仍使用 `System.Random`；不阻断本轮窗口实测。

---

## 2026-07-13 | ACT1 C0/C1 QA 硬编码门禁失败与 C2 分派

> QA 配置更新：用户指定 QA质量保证部固定使用 `gpt-5.6-sol`、推理强度 `max`。C2 已通过原 QA2 自动化范围复核，但在进入用户窗口实测前，已按新配置启动 `ACT1-FEEDBACK-QA3-SOL-20260713` 独立重审；其他部门继续使用 `gpt-5.6-luna` + `high`。

### QA3 Sol/max 追加结论
- [ ] 自动化门禁不可放行。QA3 发现敌人 Definition 与 Core 结算存在双真源：碎石护体破除后 UI 仍预告 7、Core 实际改为 12；掠灵散修护体/符火和相柳阶段阈值也散落具体数值。
- [ ] 空/越界意图仍存在 7/13/0 静默 fallback，Core 日志仍写具体机制名。
- [ ] 奖励入口在缺失 ActiveNode 时生成 `card/lingyun:unknown`，可绕过节点上下文写入永久状态。
- [ ] RunState 固定 `act1_l0_n0`，MapRenderer 缺显示名时回退内部 NodeId。
- [x] 已登记 BUG-20260713-012~014、R-050~052、ACT1-HC-005~008，并向开发部下发 `ACT1-FEEDBACK-C3-20260713`；修复后由 `gpt-5.6-sol` + `max` 执行 QA4。

### QA4 Sol/max 追加结论
- [ ] 自动化门禁仍不可放行：阶段切换后使用全局回合取模，相柳/山魈王会跳过或重复阶段意图。
- [ ] 山野妖猴“嬉斗”与 7/13 在同一循环，第四回合会再次嬉斗，偏离冻结规则。
- [ ] 掠灵“符火”实际获得 10 护体但摘要未展示；嬉斗也未完整说明机制激活。
- [ ] `Shop.tscn` 写死“行脚宝商”；未知意图类型、0 伤害攻击和阶段定义缺失的拒绝仍不完整。
- [x] C3 已解决：`unknown` 奖励、固定起点、NodeId 文案兜底以及敌人数值在 Core 的直接补写。
- [x] 已登记 BUG-20260713-015~017、R-053~055、ACT1-HC-009~012，并向开发部下发 `ACT1-FEEDBACK-C4-20260713`；修复后由 Sol/max 执行 QA5。

### 已通过的功能证据
- [x] C0/C1 Debug/Release build 0 错误 0 警告，Godot 4.7 Mono headless 退出码 0，四项自检 PASS。
- [x] 500 seeds：生成失败 0、交叉 0、路径 4..9、商店 2..3、混合层最少 2 类且至少 1 个战斗节点。
- [x] 地图无交叉/混合层、猴闪避延迟激活、双敌人绑定、意图摘要、右键取消源码与 Debug 一键胜利隔离均通过 QA 的源码/headless 范围复核。

### QA P0 Findings
- [ ] `GameManager` 仍按具体 `wuzhu` ID 绑定初始牌组，Core 未完全消费角色 Definition。
- [ ] `BattleController` / `CardRewardController` 仍直接写死“符修遗箓”奖励来源名称。
- [ ] QA 因上述生产硬编码判定不可放行；build/headless PASS 不覆盖门禁失败。

### 追加 Findings 与分派
- [ ] R-039 继续保留：普通奖励 `System.Random` 与 RewardId 幂等不在本轮完成范围。
- [ ] 非法灵韵奖励区间当前记录错误后返回 0 并继续结算，登记 BUG-20260713-011 / R-049。
- [x] 已向开发部下发 `ACT1-FEEDBACK-C2-20260713`，要求数据驱动初始牌组、奖励来源上下文、非法奖励显式失败，并补双角色/双来源/异常区间自检；完成后 QA 从硬编码门禁重新复核。

---

## 2026-07-13 | ACT1 首轮窗口实测反馈与返工分派

### 用户反馈与代码核对
- [ ] 地图分支连线发生交叉、整体杂乱。核对确认生成器对每层路线槽独立 Shuffle，Validator 没有平面无交叉约束。
- [ ] 某一层全部为灵脉。核对确认 `MapGraphGenerator.ResolveNodeMetadata()` 将 Layer 5 全部硬编码为 Lingmai，属于用整层单一节点满足逐路径配额的短视实现。
- [ ] 山野妖猴开局即显示闪避。核对确认 BattleState 对所有敌人把 `MonkeyDodgeCounter` 初始化为 0，伤害和 UI 仅以 `>=0` 判断，机制未绑定猴子且未等待“嬉斗”激活。
- [ ] 前三战显示名均为山野妖猴。核对确认 `BattleController.BuildEnemyArea()` 把 portraitLabel 写死为“山野妖猴”，实际 EnemyInfo/出招可能已经不同。
- [ ] “伏石”等意图无法理解。当前“伏石”实际为获得 8 护体，但 UI 对强化类只显示名称，不展示 Guard/Vulnerable/特殊效果。
- [ ] 点击选中后右键无法取消。当前 CardButton 仅在 pressing/dragging 时处理右键，Battle 根 `_GuiInput` 不能稳定覆盖 selected 状态。
- [ ] 用户要求 Battle 一键胜利测试按钮，避免完整路线回归时逐场慢打。

### 返工原则
- 地图拓扑本身必须在固定层内顺序下无交叉，不能只用曲线、透明度或美术遮掩交叉。
- L4~L7 必须是混合节点层；逐路径配额与层内多样性同时成立。
- Battle UI/机制全部消费 ActiveEncounter.EnemyInfo 和显式 mechanic 状态，不保留具体敌人名称假设。
- 一键胜利只服务 Debug 活动战斗，保留 HP/套牌/地图/seed，复用真实胜利、奖励和 NodeResult 链；Release 完全隔离。

### 管理登记
- 新增 BUG-20260713-003~008 和 P1-018。
- 新增 ACT1-FB-001~008 窗口反馈验收项。
- 新增 R-042~R-047，分别记录地图平面性、层分布、敌人残留假设、意图可读性、右键取消和测试工具隔离风险。
- 根目录 `AGENTS.md` 已补“内容与规则硬编码”红线：控制器不得出现具体内容名称/ID，混合层不得用固定层号分支写死类型，完成回报必须附硬编码扫描与多内容反证。
- 用户进一步要求把硬编码检查提升为 QA 第一优先级。已更新 `AGENTS.md` 的 QA 门禁并通知 QA质量保证部：先做硬编码扫描、允许/禁止命中分类和双内容反证，再判断功能、构建或体验是否通过；影响玩法、路线、奖励或内容绑定的生产硬编码按 P0 阻断。
- 已并线下发 `ACT1-RULES-REV2-20260713`（策划）、`ACT1-UX-REV2-20260713`（UI/UX）和 `ACT1-FEEDBACK-C0-20260713`（开发）；地图算法实现等待规则与交互口径回收后单独下发，避免并线修改同一文件。
- 已建立 heartbeat `act1`（ACT1 首轮反馈返工监控），负责回收部门输出、下发地图 C1、触发 QA 硬编码优先复核，并在收口后删除。

---

## 2026-07-13 | ACT1 Phase B2 QA 条件放行与窗口验收准备

### QA 结论
- [x] B2 QA 条件通过：生产路线、EncounterPool、7 个敌人、Shop/Event 最小节点页、非法图防御和场景加载均通过独立复核。
- [x] 生产地图已统一为 `RunState.MapGraph`，未发现 `DataDefs.MapLayers` 静态 fallback 或旧单一妖猴生产引用。
- [x] 500 seeds：图失败 0、路径 4..9、商店 3..3、图 fingerprint 500；遭遇节点 8,000、遭遇 fingerprint 500。
- [x] Debug/Release build 均 0 错误 0 警告；Godot 4.7 mono headless 退出码 0，`StateContractSelfCheck`、`MapGraphRngSelfCheck`、`B2EncounterProductionSelfCheck` 各唯一 PASS，无泄漏。

### 保留边界
- [ ] R-039：普通卡牌奖励仍使用 `System.Random`，`GrantReward()` 尚未记录已消费 RewardId；不能将奖励系统标记为稳定复现或幂等。
- [ ] 商店暂未开放法宝货架、重列和除牌服务；B2 只交付可进入、可结算、可离开的最小节点页。
- [ ] 尚未做可见窗口全路径验收，不能把 headless/场景加载替代真实鼠标操作、overlay 输入隔离和 Boss 实战验证。

### 用户窗口验收路径
- [ ] `Title -> CharacterSelect -> Map -> L1-L3 弱怪 -> L4-L7 强怪/灵脉/商店/事件 -> L8 相柳 Boss -> 奖励 -> 返回地图`。
- [ ] 验证路线节点可点、地图与套牌 overlay、三类战斗遭遇、两类事件、商店基础操作、Boss 结算、灵脉返回和异常入口恢复。

---

## 2026-07-13 | ACT1 Phase B1 QA 收口与 Phase B2 分派

### B1 独立复核结论
- [x] QA 复核 R-038：Lingmai 的缺失或错误 ActiveNode 使用无副作用恢复，不提交正常 Exited、不推进路线、不写完成集合；headless 覆盖异常入口。
- [x] QA 复核 R-040：Battle/Lingmai UI 不再直接写 `CurrentState`，状态迁移集中在 GameManager 的结果提交与离场端口。
- [x] 稳定随机基础设施通过：SHA-256 seed 派生、SplitMix64 初始化、xoshiro256** 和 Map/Encounter/Event/Shop/Reward/Combat 六条命名流。
- [x] Combat 初始洗牌/重洗按 `NodeId + ShuffleIndex` 派生局部流，永久套牌不被洗牌。
- [x] 9 层纯数据 MapGraph/Generator/Validator 通过 500 seeds：失败 0、路径 4..9、商店 3..3、尝试次数 1/1.00/1、不同 fingerprint 500。
- [x] Debug/Release build 均 0 错误 0 警告；Godot 4.7 mono headless 输出唯一 `StateContractSelfCheck PASS` 与 `MapGraphRngSelfCheck PASS`，退出码 0，无 ObjectDB 泄漏。

### 范围边界与 QA Findings
- [ ] Reward 流当前只有稳定 API；生产 CardReward 仍使用 `System.Random`，`GrantReward()` 也未持久化消费过的 RewardId。该项登记为 R-039，不把 B1 的基础设施 PASS 表述为完整奖励生产接入。
- [ ] MapGraphValidator 还未显式拒绝重复边/重复 NodeId。该项登记为 R-041，B2 生产切换前必须补齐。
- [x] B1 不替换生产静态地图。MapController/MapRenderer 仍使用旧图是刻意阶段边界，不作为 B1 失败；切换必须在 B2 与真实节点内容一起原子完成。

### B2 分派
- [x] 已向开发部下发 `ACT1-PHASE-B2-20260713`：实现 EncounterPool、三弱怪、三强怪和相柳之骸，并把 MapGraph 与所有真实节点入口原子接入生产。
- [x] 明确禁止：静态 `DataDefs.MapLayers` 与运行图双真源、展示空白 Shop/Event、自动跳过节点或用假成功绕过结算。
- [x] Shop/Event 若尚未具备真实最小页面与 NodeResult，生产 MapGraph 不得启用；地图、套牌、设置继续作为 overlay，节点页作为底层。

### 管理文档更新
- [x] `任务看板.md`：P1-012 收口，P1-013~015 进入 B2 实施。
- [x] `验收清单.md`：新增 B1-001~006 门禁，并将 ACT1-001~005 标为基础设施部分通过、等待生产接入。
- [x] `风险记录.md`：R-038/R-040 关闭；R-039 更新为生产奖励风险；新增 R-041。
- [x] `项目上下文快照.md`：记录 B1 QA 结果、B2 范围和监控回收状态。

---

## 2026-07-13 | ACT1 Phase A Rev1 通过 QA

### Rev1 修复结果
- [x] 新增 `NodeResultType`、唯一 `ResultId`、`ConsumeNode`、`AdvanceRoute`，由核心真值表校验结果语义。
- [x] 进入节点不再提前推进地图；只有合法结果且 `AdvanceRoute=true` 时一次性应用节点坐标。
- [x] 合法 Completed/Exited/Skipped/Defeated/Abandoned 结果提交均返回成功；重复 ResultId 和任一终态覆盖均拒绝。
- [x] Battle/Lingmai 使用统一离场 API 清理 BattleState、ActiveEncounter、ActiveNode 和流程状态。
- [x] Lingmai 消费 ActiveNode；正常成功和返回不再从 CurrentMapLayer/Index 猜节点。
- [x] 分离战斗洗牌；StartNewRun 不洗永久套牌，奖励加牌不改变活动战斗抽牌堆。
- [x] 状态契约自检失败时非零退出；正常输出唯一 `[StateContractSelfCheck] PASS`。

### QA 二次复核
- ARCH-001~007：全部 Pass。
- Debug/Release build：0 错误、0 警告。
- Godot 4.7 mono headless：唯一 PASS，退出码 0，无“状态契约自检失败”和 ObjectDB 泄漏。
- 连续两战：第二战起手 5、总卡数 11、弃牌/消弭为空，HP 与永久套牌保持。

### 保留风险
- P1：Lingmai invalid-entry 恢复可能卡死或先推进后离场失败，Phase B 开工前加固。
- P2：`CurrentState` 仍有 UI/核心双写，后续迁入统一流程协调。
- P2：奖励防重仍主要依赖 UI；Reward 流接入时必须增加 RewardId 与幂等领取。
- 遭遇池尚未实现；Phase A 只保证 Battle 消费显式 EnemyId/EncounterRequest，不代表 P1-014 已完成。

### 当前状态
- Phase A：Done，可放行。
- P1-012：Doing（剩余六条独立随机流）。
- BUG-20260713-002：Done。
- 下一步：Phase B 实现稳定随机流与 9 层 MapGraph 生成器，随后再接 6 种敌人和相柳。

---

## 2026-07-13 | ACT1 Phase A 首轮 QA 不通过与 Rev1 返修

### QA 独立结果
- [x] Debug/Release build：0 错误、0 警告。
- [x] QA 定位并运行 Godot 4.7 .NET headless；状态契约自检实际失败，但进程错误返回 0。
- [ ] ARCH-003：第一战提交结果并销毁 BattleState 后，`CurrentState` 仍为“战斗中”，第二次 `TryEnterBattle()` 被拒绝。
- [ ] ARCH-005：失败/跳过结果缺少完整幂等语义；失败状态写入后返回 `false`，`Skipped/Abandoned` 可被后续覆盖。
- [ ] ARCH-006：被连续两战失败阻断，未完成动态验证。

### 总项目经理规则裁决
- 灵脉不选行动使用 `Exited/NodeSkipped` 结果，运行时终态为 `Skipped`；`ConsumeNode=false`、`AdvanceRoute=true`，不发奖励、不进 Completed/Visited，但允许前往下一层。
- 灵脉成功和战斗胜利为 `Completed`、`ConsumeNode=true`、`AdvanceRoute=true`。
- 战斗失败/放弃为 `Defeated/Abandoned`、`ConsumeNode=false`、`AdvanceRoute=false`，不得因进入节点时提前写地图位置而跳过战斗。
- 进入节点只创建 ActiveNode；地图位置只在幂等结果应用时依据 `AdvanceRoute` 更新。

### Rev1 分派
- [x] 已向开发部下发 `ACT1-PHASE-A-REV1-20260713`。
- [ ] 引入 `NodeResultType / ResultId / ConsumeNode / AdvanceRoute`，统一合法终态提交返回值和重复拒绝。
- [ ] 统一 Battle/Lingmai 离场端口，修复流程状态、活动上下文和地图位置提交顺序。
- [ ] Lingmai 改为消费 ActiveNode，不再从 CurrentMapLayer/Index 猜入口。
- [ ] 分离战斗洗牌与永久套牌；奖励加牌不得改变活动战斗抽牌堆。
- [ ] 强化连续两战、引用隔离、灵脉退出、终态幂等自检；失败必须非零退出，成功输出唯一 PASS。

### 当前状态
- P1-012 / BUG-20260713-002：Doing。
- Phase A 未放行；P1-013~P1-015 继续等待。

---

## 2026-07-13 | 总项目经理 ACT1 架构审查、调试回退与 Phase A 分派

### 用户方向
- [x] 撤回最近新增的全局 F10 Debug 面板，不再用“重新初始化角色”的快捷入口掩盖节点耦合。
- [x] 生命、永久套牌、节点页面和战斗实例必须相互解耦；任意节点切换保留当前生命与永久套牌，只销毁对应节点的临时状态。
- [x] 启动完整第一大关设计与实现：3 种弱怪、3 种强怪、关底 Boss、每局随机路线、商店和至少 2 种问号事件。
- [x] 节点页面是底层；地图、套牌和设置是统一呼出层。
- [x] 部门任务统一使用 `gpt-5.6-luna`、高推理。

### Debug 回退结果
- [x] 开发部删除 `DebugService` Autoload、F10 面板场景/控制器、`DebugForceVictory` 和模拟胜利消费逻辑。
- [x] 保留正式 Map/Lingmai 流程、`OpenMapOnEnter` / `LastLingmaiResult` 迁移兼容和 F9 手牌布局调参。
- [x] Debug/Release build 均为 0 错误、0 警告；实现、场景和工程配置中相关残留引用为 0。

### 总项目经理与 QA 独立审查
- [x] 确认 `PlayerHp` 当前会跨普通场景保留，但只是全局字段副作用，不是显式契约。
- [x] 确认永久套牌与 Draw/Hand/Discard/Exhaust 混用，胜利时残留手牌会污染下一场战斗。
- [x] 确认 Map 只写全局字段、Battle 无条件初始化，缺少 NodeContext/EncounterRequest/Result。
- [x] 确认敌人回合、战斗 UI、日志和胜利文案直接绑定 `DataDefs.MountainMonkey`，地图 EnemyId 未生效。
- [x] 确认战斗节点进入时即写入已访问，失败/放弃无法与胜利区分。
- [x] 确认 Lingmai 缺少共享 TopBar，Map/Battle 存在两套地图绘制与交互实现。

### 冻结方案与分派
- [x] `RunState` 只保存当前/最大生命、基础最大灵力、永久套牌、灵韵、道痕/法宝、Act/Seed 和地图进度。
- [x] `BattleState` 保存当前回合灵力、护体、斗劲、临时状态、战斗牌堆和敌人运行态；离开战斗销毁。
- [x] NodeContext/NodeResult 负责节点显式输入输出；节点成功提交后才完成，跳过/放弃/失败分别可辨且结果幂等。
- [x] 开发部已接收 `ACT1-PHASE-A-20260713`，先做状态端口、战斗生命周期和显式遭遇入口，不提前实现路线/商店/事件。
- [x] 策划部已完成 `03-玩法系统/11-第一大关设计.md`；UI/UX部已完成 NodePage/overlay 契约；QA 已完成只读架构审查。
- [x] 已创建 `act1-phase-a-monitor` heartbeat，Phase A 与 QA 收口、下一阶段下发后立即删除。

### 当前状态
- P1-011 / BUG-20260713-001：Done（按用户决策撤回，不再窗口复测）。
- P1-012：Doing（Phase A 架构端口改造中）。
- BUG-20260713-002：Doing（连续战斗残留手牌污染随 Phase A 修复）。
- P1-013~P1-015：Ready，必须等待 Phase A + QA 通过后按阶段启动。

---

## 2026-07-13 | UI/UX部 ACT1-UI-CONTRACT-20260713 节点页面与状态边界

### 完成事项
- [x] 在 `06-UI交互/00-UI交互总览.md` 补充统一 NodePage 壳层、TopBar、Map/Deck/Settings overlay 的层级、输入拦截和 toggle 规则。
- [x] 在 `06-UI交互/01-MVP界面交互.md` 补充 BattleNode、LingmaiNode、ShopNode、EventNode、BossNode 的页面结构和状态机。
- [x] 明确节点入口/退出契约：UI 读取 `RunStateSnapshot` 与 `NodeContext`，触发命令并返回 `NodeResult`；不得调用 `InitCharacter` 或隐式清空角色状态。
- [x] 修正状态边界：RunState 只保留当前/最大生命、基础最大灵力、永久套牌、灵韵、道痕/法宝、Act/Seed/地图进度；当前回合灵力、护体、斗劲、易损/永炎、战斗牌堆和敌人运行态归 BattleState。
- [x] 明确灵脉“不选行动”返回 `Skipped/Exited` 且 `consume_node=false`，UI 不直接写 `VisitedNodeIds`。
- [x] 补充第一大关 19 项可见窗口验收，覆盖所有节点页面的地图/套牌 overlay 一致性和跨节点状态保留。

### 当前冲突与迁移建议
- 当前代码仍由 `GameManager` 同时承载 RunState/BattleState，Map/Battle 各自创建 TopBar/overlay，Shop/Event/Boss 尚无页面；本次仅记录 UI 契约，不修改代码。
- 开发部建议先抽 `NodePageRoot + OverlayHost + TopBar`，再以兼容适配迁移 Battle/Lingmai，最后接入 Shop/Event/Boss；不在本轮重写全部全局状态。

### 验证结果
- 本次仅修改 `洪荒之后-main/docs/06-UI交互/` 及本开发记录，未修改 `洪荒之后-code/`、Godot 场景或美术资源。
- 未运行 `dotnet build`；本轮没有代码改动。

---

## 2026-07-13 | BUG-20260713-001 Debug 面板跨场景后 F10 无法再次打开

### 用户反馈
- [ ] 调试面板第一次使用按钮跳转场景后，再按 F10 没有反应；用户误以为调试选项只能使用一次。

### 初步判断
- DebugService 是 Autoload，但当前输入回调使用 `_UnhandledInput`，需要核对场景切换后输入处理、ProcessMode 和根窗口面板引用是否仍有效。
- 调试入口本身不应一次性；一次性只适用于 DebugForceVictory、OpenMapOnEnter 等状态标记。

### 分派与验收
- 已分派开发部：确保 DebugService/DebugPanel 使用 Always 生命周期，F10 可重复切换，关闭后可重新打开，地图/战斗/灵脉/胜利奖励按钮可重复调用，Release 隔离不变。
- 验收覆盖：第一次打开 → 点击入口跳场景 → F10 再开/关 → 再次点击入口；Debug/Release build。

### 当前状态
- BUG-20260713-001：Review。
- 开发部已完成修复，等待用户窗口实机复测。

### 开发部回收结果
- [x] DebugService 改用 `_Input` 接收 F10，并设置 `ProcessModeEnum.Always`。
- [x] DebugPanelController 和面板场景设置 Always 处理模式；跨场景仍保持有效。
- [x] 场景跳转入口先隐藏面板但不释放；F10 或关闭按钮后可再次打开，所有调试入口可重复使用。
- [x] Release 隔离保持不变。

### 验证结果
- `dotnet build .\\AfterHongHuang.csproj`：成功，0 个错误、0 个警告。
- `dotnet build -c Release .\\AfterHongHuang.csproj`：成功，0 个错误、0 个警告。
- Godot headless：未运行，PATH 中未找到 `Godot_v4.7-stable_mono_win64_console.exe`。
- 用户待测：打开面板 → 跳转场景 → 再按 F10；关闭后再次按 F10；连续重复调试入口。

---

## 2026-07-12 | P1-011 开发期调试接口与快捷面板分派

### 用户需求
- 不再为了测试灵脉、战斗奖励和地图流程反复从标题开始并击败小怪。

### 目标行为
- Debug 构建按 F10 打开全局调试面板，面板跨场景保留；Release 构建不显示、不响应、不执行调试入口。
- 提供重置测试状态、直接打开地图、进入战斗、进入灵脉、模拟真实战斗胜利/奖励、补满生命/灵力等入口。
- 模拟胜利必须消费一次性调试标记并调用真实战斗胜利判定、灵韵奖励和 CardReward 流程，不复制假奖励界面。

### 分派
- 已分派开发部新增 DebugService、DebugPanel 场景/控制器，必要时补 GameManager 调试标记和 BattleController 一次性消费逻辑；保持正式玩法与 Debug 逻辑隔离。
- 验收覆盖：F10 入口、跨场景状态显示、地图/战斗/灵脉快捷进入、真实胜利奖励、重置、补充状态和 Release 隔离。

### 当前状态
- P1-011：Review。
- 开发部已完成实现，等待用户窗口实机复测。

### 开发部回收结果
- [x] 新增 `DebugService` Autoload、`DebugPanel.tscn` 和 `DebugPanelController.cs`；Debug 构建按 F10 打开/关闭，面板挂在根窗口下跨场景保留。
- [x] 提供重置、直接地图、直接战斗、直接灵脉、模拟真实胜利奖励、补满生命/灵力等入口；按钮只转发到 DebugService。
- [x] `DebugForceVictory` 仅在 Debug 构建且运行时 Debug 环境消费，设置敌人生命为 0 后调用真实 CheckBattleResult/胜利奖励流程。
- [x] Release 构建不创建面板、不响应 F10、不消费模拟胜利标记。

### 验证结果
- `dotnet build .\\AfterHongHuang.csproj`：成功，0 个错误、0 个警告。
- `dotnet build -c Release .\\AfterHongHuang.csproj`：成功，0 个错误、0 个警告。
- Godot headless：未运行，PATH 中未找到 `Godot_v4.7-stable_mono_win64_console.exe`。
- 用户待测：F10 面板、跨场景、真实胜利奖励和灵脉入口。

---

## 2026-07-12 | P1-010 灵脉独立场景化与 Map 底页移除分派

### 用户决策
- 删除 MapScene 中战后当前节点摘要底页；该页面不再作为主流程入口。
- 灵脉节点采用与战斗一致的独立场景切换：MapScene → LingmaiScene → MapScene。
- LingmaiScene 提供灵脉互动按钮；成功选择或返回地图后关闭/离开该页面。

### 目标与边界
- 初始未固化道痕时的道韵选择页面保留；删除的是已选道痕后的当前节点摘要底页。
- MapController 只负责地图绘制、节点位置和场景跳转；灵脉选项、卡牌升级选择和结算迁移到 LingmaiController。
- 灵脉成功行动才加入已访问；返回地图不消耗灵脉但允许继续下一节点。

### 分派
- 已分派开发部新增 Lingmai.tscn 与对应控制脚本，迁移旧灵脉 overlay 逻辑，清理 MapController 中旧灵脉入口，统一一次性自动开图状态。
- 验收重点：场景切换、按钮操作后页面关闭、返回地图自动展开、灵脉跳过与成功结算两条路径、初始道痕选择和战斗中地图只读无回归。

### 当前状态
- P1-010：Review。
- 开发部已完成实现，等待用户窗口实机复测。

### 开发部回收结果
- [x] 新增 `Lingmai.tscn` 与 `LingmaiController.cs`，灵脉选项、升级选卡、返回地图和结算已迁移到独立场景。
- [x] MapScene 已选道痕后的当前节点摘要底页已移除；初始道韵选择页保留。
- [x] 战斗返回和灵脉返回统一使用一次性 `GameManager.OpenMapOnEnter` 自动展开地图。
- [x] 灵脉返回/升级取消不消耗节点，成功休养生息或精进道行才加入 `VisitedNodeIds`。

### 验证结果
- `dotnet build .\\AfterHongHuang.csproj`：成功，0 个错误、0 个警告。
- Godot headless：未运行，PATH 中未找到 `Godot_v4.7-stable_mono_win64_console.exe`。
- 用户待测：战斗返回地图、地图进入灵脉场景、选项收口、返回地图自动开图和下一节点可达。

---

## 2026-07-12 | BUG-20260712-002 / BUG-20260712-003 战后地图与灵脉前进流程登记

### 用户反馈
- [ ] 战斗结束领奖励后点“继续”进入只有当前节点摘要的页面，没有直接展开地图卷轴。
- [ ] 灵脉节点不选择行动、点击“返回地图”后无法继续前往下一节点。

### 目标行为
- 战后进入 MapScene 后自动调用现有地图打开逻辑，直接显示地图卷轴；当前节点摘要不再作为战后流程中间页。
- 进入灵脉时立即推进当前位置，但不加入已访问；返回地图可以继续下一层，只有成功选择休养生息或精进道行等行动后才标记灵脉已访问。

### 分派
- 已分派开发部修改 MapController 初始化与灵脉进入状态；不得复制地图 UI，不得破坏初始道痕选择、战斗中地图只读和灵脉成功结算规则。
- 验收覆盖：胜利奖励 → 继续 → 自动展开地图；灵脉返回地图不选行动 → 下一节点可点击；灵脉成功行动 → 节点消耗且不可重复领取。

### 当前状态
- `BUG-20260712-002` / `BUG-20260712-003`：Review。
- P1-009 恢复 Review，等待用户窗口实机复测。

### 开发部回收结果
- [x] `MapController._Ready()` 在胜利返回 MapScene 后通过 `CallDeferred` 自动调用现有 `OpenMap()`；自动开卷标记只消费一次，不影响初始道痕选择。
- [x] `EnterLingmaiNode()` 立即更新当前位置，但不写入 `VisitedNodeIds`；返回地图后按灵脉节点计算下一层可达性。
- [x] 灵脉说明已明确允许返回地图继续前行；成功休养生息或精进道行仍由结算函数标记节点访问。

### 验证结果
- `dotnet build .\\AfterHongHuang.csproj`：成功，0 个错误、0 个警告。
- Godot headless：未运行，PATH 中未找到 `Godot_v4.7-stable_mono_win64_console.exe`。
- 用户待测：胜利继续自动开地图；灵脉返回地图不选行动可走下一节点；成功行动后灵脉不可重复领取。

---

## 2026-07-12 | BUG-20260712-001 灵脉入口流程缺陷登记与分派

### 用户反馈
- [ ] 从战斗胜利/卡牌奖励流程点击“继续”后，点击灵脉节点没有出现灵脉选项界面，界面返回并停留在之前的卡牌奖励/胜利层。

### 根因确认
- `BattleController.ShowVictoryPopup()` 的“继续”按钮当前调用 `ShowMapOverlay()`，仍留在 `Battle.tscn`。
- `BattleController.OnBattleMapNodePressed()` 的灵脉分支只更新访问状态并关闭战斗地图 overlay，没有调用 `MapController.EnterLingmaiNode()`；真正的灵脉界面只存在于 `Map.tscn`。

### 分派与验收
- 已分派开发部：将胜利结算“继续”改为进入独立 `Map.tscn`，确认旧 victory/CardReward overlay 不残留；不得在 BattleController 复制灵脉 UI；复用 MapController 灵脉入口并完成 `dotnet build`。
- 用户复测路径：标题开始 → 固化道痕 → 进入战斗并获胜 → 领取/跳过卡牌奖励 → 继续 → MapScene → 点击可达灵脉 → 出现灵脉选项 → 完成一次选项并返回地图；同时回归战斗中 TopBar 地图只读行为。

### 当前状态
- `BUG-20260712-001`：Review。
- P1-009 恢复 Review，等待用户窗口实机复测。

### 开发部回收结果
- [x] 胜利结算“继续”改为调用 `GoToMap()`，切换到独立 `Map.tscn`，不再复用战斗中的只读地图 overlay。
- [x] 切场景前清理战斗地图、胜利弹窗和 `CardReward` overlay；奖励 overlay 增加稳定名称与父节点清理接口。
- [x] 战斗中 TopBar 地图保持只读，节点回调不再修改访问状态或绕过 `MapController` 结算流程。
- [x] `MapScene` 继续由 `MapController` 负责 `EnterLingmaiNode()` → `ShowLingmaiOptions()`，灵脉成功结算后才消耗节点。

### 验证结果
- `dotnet build .\\AfterHongHuang.csproj`：成功，0 个错误、0 个警告。
- Godot headless：未运行，PATH 中未找到 `Godot_v4.7-stable_mono_win64_console.exe`。
- 用户待测：战斗胜利 → 领取/跳过卡牌奖励 → 继续 → MapScene → 点击灵脉 → 看到并完成灵脉选项。

---

## 2026-07-12 | BUG-20260711-001 修复回收

### 修复结果
- [x] **根因确认**：`MapController.BuildInteractiveMap()` 的节点按钮直接挂在普通 `Control` 下，只设置 `CustomMinimumSize` 不会自动获得实际点击尺寸。
- [x] **最小修复**：为道韵、战斗、灵脉和 Boss 全部地图按钮补充 `Size = new Vector2(NodeWidth, NodeHeight)`，当前实际尺寸为 `240×90`。
- [x] **交互链核对**：`MapRenderer.IsNodeAccessible()` 的灵脉可达判定正常；`OnMapNodePressed()` → `EnterLingmaiNode()` 回调链正常；没有修改灵脉成功结算后才标记访问的规则。

### 验证结果
- `dotnet build .\AfterHongHuang.csproj`：成功，0 错误、0 警告。
- Godot headless：未运行，PATH 中未找到 `Godot_v4.7-stable_mono_win64_console.exe`。
- 待用户实机确认：地图卷轴完全展开后，点击可达灵脉节点能够打开“灵脉节点”选项 overlay。

### 状态
- `BUG-20260711-001` 从 Blocked 调整为 Review。
- `bug-20260711-001-lingmai-click-monitor` 已删除。

---

## 2026-07-12 | BUG-20260711-001 开发环境阻断收口

### 阻断事实
- 开发部针对同一最小修复连续两次尝试均失败于 `CodexSandboxOffline / helper_sid_resolve_failed`。
- 两次尝试均未能读取、写入工程或运行 `dotnet build`；没有代码改动，也没有伪造验证结果。

### 当前处置
- `BUG-20260711-001` 从 Doing 调整为 Blocked。
- 已删除 `bug-20260711-001-lingmai-click-monitor`，避免在开发环境未恢复前持续空转。
- 恢复条件：开发部文件系统环境恢复，或用户明确授权总项目经理直接在 `MapController.BuildInteractiveMap()` 补齐节点按钮实际 `Size` 并执行验证。

---

## 2026-07-11 | BUG-20260711-001 灵脉节点点击无反应登记

### 用户反馈
- [ ] 用户实机点击地图上的灵脉节点没有反应，因此无法进入灵脉 overlay，后续休养生息、精进道行和节点不可重复领取暂无法验收。

### 初步排查
- `MapController.BuildInteractiveMap()` 创建地图节点按钮时使用了 `CustomMinimumSize`，但没有显式设置 `Size`；这些按钮直接挂在 `Control` 上而非 `Container`，存在实际点击区域为 0 或不符合预期的风险。
- 同时要求开发部检查 `MapRenderer.IsNodeAccessible()` 的当前层/已访问/地图动画锁，以及 `OnMapNodePressed()` 回调绑定，避免只修表面尺寸。

### 分派
- 开发部：确认根因并修复地图灵脉节点的实际点击区域、可达性和回调链；只修改 `洪荒之后-code/` 必要文件，补 `dotnet build` 和代码侧回归。
- 用户：修复后复测从战斗胜利返回地图，点击可达灵脉节点能打开 overlay。

### 当前状态
- `BUG-20260711-001`：Doing。
- P1-009 保持 Review，但被该 BUG 阻断，不能宣布灵脉功能验收完成。

---

## 2026-07-11 | P1-008 / P1-009 实现回收

### 部门结果
- [x] **UI/UX部**：已补齐角色选择 A/B/C 区调整口径和灵脉节点 overlay 流程；未修改代码或资源。
- [x] **策划部**：已补齐灵脉节点规则、术语替换、精进道行升级规则、疗愈道友和法宝触发选项边界；未修改代码或资源。
- [x] **开发部**：已在 `洪荒之后-code/` 实现角色选择布局微调、灵脉 overlay、休养生息、精进道行、疗愈道友禁用和节点结算状态，并更新 `洪荒之后-code/docs/开发记录-code.md`。

### 开发部修改文件
| 文件 | 说明 |
|------|------|
| `scenes/CharacterSelect/CharacterSelect.tscn` | 下移角色栏和开始按钮 |
| `scripts/UI/CharacterSelectController.cs` | C 区统计收敛为生命显示 |
| `scripts/UI/MapController.cs` | 灵脉 overlay、选项状态、结算与返回地图 |
| `scripts/Core/GameManager.cs` | 可升级卡查询与升级替换接口 |
| `scripts/Data/DataDefs.cs` | 巫祝初始 4 种卡的 `_p` 升级版数据 |
| `docs/开发记录-code.md` | 代码侧开发记录 |

### 验证结果
- 开发部回报 `dotnet build .\AfterHongHuang.csproj` 成功，0 错误 0 警告。
- Godot headless 未运行：开发部在本机 PATH、工作区、`C:\Users\ASUS`、`C:\Program Files`、`C:\Program Files (x86)` 未找到 `Godot_v4.7-stable_mono_win64_console.exe`。

### 当前状态
- `P1-008` 与 `P1-009` 从 Doing 调整为 Review。
- 待用户窗口实机复测：角色选择 A/B/C 区位置与简化统计、灵脉入口、休养生息回血上限、精进道行选卡升级、返回地图后节点不可重复领取。
- 监控自动化 `p1-008-009-role-select-and-spirit-vein-monitor` 已删除，避免空转。

---

## 2026-07-13 | 策划部 ACT1-DESIGN-20260713 第一大关完整玩法规格

### 启动检查与已读文档
- [x] 已重新读取根目录 `AGENTS.md`，确认当前角色是策划部，只修改 `洪荒之后-main/docs`。
- [x] 已读取 `项目上下文快照.md`、`任务看板.md`，确认 P1-009 灵脉流程已进入 Review，ACT1 规格来自总项目经理任务卡。
- [x] 已读取世界观总览、战力金字塔、巫祝、卡牌系统、战斗系统、地图与路径、灵脉、法宝、局内经济、事件、关卡、Boss 和验收清单。
- [x] 已参考 Steam 商店公开页及 Mega Crit 官方 `Major Update #2 - v0.107.1` RNG Rework 公告；只采用公开的主 RunSeed + 派生独立随机流原则，不假设未公开地图算法。

### 完成事项
- [x] **敌人池**：确定 3 个弱怪、3 个强怪、1 个 ACT1 MVP Boss；弱怪/强怪/组合遭遇均定义为数据池，不绑定场景。
- [x] **路线规格**：确定 9 层分层 DAG、前三层弱怪、Layer 4~7 混合节点、Layer 8 Boss、节点配额、相邻限制、有效路径 4~12 和无死路双向检查。
- [x] **独立随机流**：明确 Map、Encounter、Event、Shop、Reward、Combat 六条由 ActSeed 派生的隔离流，额外奖励抽取不能改变地图或后续系统。
- [x] **商店**：统一 ACT1 货币为灵韵，补齐库存类型、价格、随机抽取、重列货架、除牌、购买状态和离开规则。
- [x] **问号事件**：补齐“天河倒灌”和“散修遗篆”两种事件，包含条件、选项、收益/代价、生命/牌组影响、随机分支和不可用条件。
- [x] **节点页面契约**：明确节点页面为底层，地图/套牌为呼出层，非 MapScene 地图只读，节点场景通过 NodeContext/NodeResult 读写运行状态。
- [x] **状态边界收口**：修正 RunState 只保存 HP、基础最大灵力、永久套牌、灵韵、道痕/法宝、Act/Seed/地图进度；当前回合灵力、护体、斗劲、易损/永炎、战斗牌堆和敌人运行态归 BattleState，非战斗 TopBar 不显示当前回合灵力。
- [x] **Boss 天品最小池**：补齐 `wx_a1_boss_001`~`wx_a1_boss_003` 三张只使用现有字段的 ACT1 天品卡，固定 3 选 1；本轮无升级版，不进入常规奖励池。
- [x] **商店约束收口**：商店改为图级至少 1 个、建议 2~3 个可达候选；每条实际路径允许 0~1 个，每个 Layer 3 节点必须存在经过商店到 Boss 的路径。
- [x] **开发拆分**：新增 P1-012~P1-016，分别负责数据契约与随机流、路线生成、遭遇/Boss、商店/事件、完整路线验收。

### 更新文档
| 文档 | 变更 |
|------|------|
| `03-玩法系统/11-第一大关设计.md` | v1.0.0→v1.0.1，补齐 Boss 天品、图级商店候选和 RunState/BattleState 边界 |
| `03-玩法系统/08-关卡设计.md` | v0.6→v0.7.1，接入 ACT1 敌人池、路线摘要和可选商店口径，明确相柳之骸为 MVP Boss |
| `03-玩法系统/03-地图与路径.md` | v1.1→v1.2.1，接入 9 层 DAG、配额、图级商店候选、种子和生成器待实现状态 |
| `03-玩法系统/05-局内经济.md` | v0.5→v0.6.1，统一灵韵并补齐 ACT1 商店价格、服务和图级出现规则 |
| `03-玩法系统/06-事件系统.md` | 新增 ACT1 两种问号事件规格 |
| `03-玩法系统/09-Boss设计.md` | v0.6→v0.7，明确相柳之骸单体 MVP 形态、阶段数值和固定天品奖励池引用 |
| `03-玩法系统/01-卡牌系统.md` | v1.0.1→v1.0.2，新增 3 张 ACT1 Boss 天品卡的 ID、字段、TargetMode、消弭和升级边界 |
| `06-UI交互/00-UI交互总览.md` | 补齐 ACT1 节点底层、地图/套牌呼出层和输入隔离验收口径 |
| `06-UI交互/01-MVP界面交互.md` | 更新 ACT1 目标路线层数表述 |
| `验收清单.md` | 新增 ACT1-001~011，覆盖复现、路线、随机流、商店、事件、Boss 天品池和 RunState/BattleState 边界 |
| `任务看板.md` | 新增 P1-012~P1-016 并登记本轮分派 |
| `风险记录.md` | 新增 R-030~R-033，记录随机流耦合、路线死路、经济/强度和 RunState/BattleState 泄漏风险 |
| `INDEX.md` | 登记 `11-第一大关设计.md`，更新玩法文档统计 |

### 关键设计结论
- ACT1 MVP 先实现相柳之骸，不把现有 6 Boss 设计池误当成本轮全部实现范围；Boss 奖励固定使用 3 张已定义天品卡，不依赖未设计池。
- ACT1 商店使用灵韵；旧经济文档中的灵石只作为历史称呼，不并行维护第二货币。
- 法宝货架和事件中的法宝选项可以有数据契约，但法宝系统未接入时必须隐藏/禁用，不能显示假商品或假奖励。
- 组合遭遇只进入数据契约和调试样例，第一轮战斗实现先保持单敌人，降低多目标 UI 与战斗状态风险。

### 验证结果
- 已完成文档交叉检查：ACT1 主规格、地图、经济、事件、Boss、UI/UX、验收和任务看板均已互相引用。
- 已检查本轮触及文档无行尾空白。
- 未运行 `dotnet build .\\AfterHongHuang.csproj`，因为本次没有修改 C#、Godot 场景或工程文件。

### 风险、待决策和下一步
- 风险：路线配额修复、六条随机流隔离和 RunState/BattleState 分界必须由开发部通过 ACT1-001~005、ACT1-010 验收，文档规则不能替代运行时验证。
- 风险：相柳之骸 HP 120、商店价格和问号收益需要首轮实机数据校准，重点记录到店灵韵、战斗回合数和 Boss 胜率。
- 待决策：是否在 ACT1 后续版本启用其余 5 个 Boss、组合遭遇和法宝货架；本轮不阻塞单 Boss MVP。
- 下一步：开发部按 P1-012→P1-013→P1-014→P1-015 顺序实现；UI/UX部同步节点页面与 overlay 输入隔离；最后由 QA/用户执行 P1-016。

---

## 2026-07-11 | UI/UX部 P1-008 / P1-009A 口径同步

### 完成事项
- [x] **角色选择布局口径**：在 `06-UI交互/01-MVP界面交互.md` 明确 A 区底部角色栏整体下移、B 区开始游戏按钮整体下移，避免压边或遮挡。
- [x] **C 区统计简化**：明确右侧信息面板统计区只显示红心图标/占位符、冒号和当前生命/生命上限，例如 `心：80/80`；不得继续展示初始牌组、灵力、回合等冗余属性。
- [x] **灵脉节点 UI 流程**：补齐从地图卷轴进入灵脉 overlay、展示选项、选择后结算回地图、节点已访问不可重复领取的流程。
- [x] **灵脉选项分组**：明确常规选项为 `休养生息`、`精进道行`、`疗愈道友`，法宝/道器触发选项仅在满足条件时显示；不可用选项必须禁用或隐藏并说明原因。
- [x] **术语边界**：灵脉 UI 不使用 `火堆`、`遗物`、`力量` 等外部术语，统一使用《洪荒之后》体系表述。

### 验收清单同步
- `验收清单.md` 已由总项目经理登记 `UI-022`、`UI-023`、`MVP-024` 和相关系统项，本次未覆盖既有测试结论。

### 给开发部的实现约束
- 不要把灵脉做成一次性假按钮；选项不可用时必须禁用或隐藏并提供原因。
- 灵脉结算完成后要写入已访问状态，回到地图后该节点不可重复领取。
- `精进道行` 需要依赖策划确认的卡牌强化表；`疗愈道友` 与法宝/道器触发选项在规则未开放前不得伪造效果。
- 角色选择 C 区仅承载生命值统计，套牌预览或更多属性若后续需要，应作为独立区域重新设计。

### 验证结果
- 本次仅修改 `洪荒之后-main/docs` 下 UI/UX 口径与开发记录，未修改 `洪荒之后-code/`、Godot 场景或美术资源。
- 未运行 `dotnet build .\AfterHongHuang.csproj`；本轮没有代码改动。

---

## 2026-07-11 | 策划部 P1-009A 灵脉节点玩法规则

### 启动检查
- [x] 已读取根目录 `AGENTS.md`，确认当前角色为策划部，只修改 `洪荒之后-main/docs` 下玩法、验收和管理文档。
- [x] 已读取 `$honghuang-department-workflow`，确认策划部不直接修改 `洪荒之后-code/` 或 `洪荒之后-art/`。
- [x] 已读取 `任务看板.md`，确认任务来自 `P1-009 灵脉节点 MVP 交互` 和总项目经理任务卡。

### 已读文档
| 文档 | 用途 |
|------|------|
| `03-玩法系统/00-玩法总览.md` | 灵脉节点基础术语与旅程结构 |
| `03-玩法系统/03-地图与路径.md` | 地图节点类型、访问判定、灵脉节点当前占位状态 |
| `03-玩法系统/01-卡牌系统.md` | 卡牌升级规则、升级版 ID 和精进道行依赖 |
| `03-玩法系统/04-法宝系统(游戏内).md` | 法宝术语和法宝触发选项边界 |
| `任务看板.md` | P1-009 状态、负责人和验收标准 |
| `验收清单.md` | MVP / UI / 玩法验收项 |
| `风险记录.md` | R-021 灵脉节点范围膨胀风险 |

### 完成事项
- [x] **补齐休养生息规则**：回复 `floor(最大生命 × 30%)`，至少 1 点；不能超过最大生命；满血时禁用并提示原因。
- [x] **补齐精进道行规则**：选择当前牌组中 1 张可升级卡并永久替换为升级版；无可升级卡时禁用；进入选卡后取消不消耗灵脉。
- [x] **补齐疗愈道友规则**：当前 MVP 单人无队友时隐藏或禁用，若显示则提示“当前无人同行”，不允许误点、不消耗灵脉。
- [x] **补齐法宝触发选项边界**：只有拥有对应法宝时才出现或可用；示例包括下一场战斗获得 1 点斗劲、获得随机法宝、温养法宝；当前不要求开发部硬做完整法宝系统。
- [x] **明确结算后地图状态**：任一可用灵脉选项成功结算后，该灵脉节点标记为已访问并返回地图；取消或不可用选项不改变节点状态。
- [x] **术语替换**：灵脉规则中不使用火堆、休息、遗物、力量等外部原词，统一使用灵脉节点、休养生息、精进道行、疗愈道友、法宝、斗劲。

### 更新文档
| 文档 | 变更 |
|------|------|
| `03-玩法系统/00-玩法总览.md` | v0.7.0→v0.8.0，重写灵脉节点章节，补齐 MVP 固定选项、法宝触发选项和本轮范围 |
| `03-玩法系统/03-地图与路径.md` | v1.0.0→v1.1.0，灵脉节点由占位改为最小交互，补充取消/结算/访问状态 |
| `03-玩法系统/01-卡牌系统.md` | v1.0.0→v1.0.1，卡牌升级方式改为灵脉节点“精进道行”，补充规则 |
| `03-玩法系统/04-法宝系统(游戏内).md` | v0.6.0→v0.6.1，新增灵脉触发型法宝选项边界 |
| `验收清单.md` | v0.1.21→v0.1.22，细化 MVP-024、UI-023、SYS-007~SYS-009 |
| `任务看板.md` | P1-009 验收标准补充取整、禁用、节点消耗规则 |
| `风险记录.md` | R-021 更新为“已缓解，监控中”，明确不扩张队友/法宝系统 |

### 给 UI/UX部与开发部的口径
| 选项 | 效果 | 条件 | UI 处理 | 结算后地图状态 |
|------|------|------|------|------|
| 休养生息 | 回复 `floor(最大生命 × 30%)`，至少 1 点，不超过最大生命 | 当前生命低于最大生命 | 满血时禁用并显示原因 | 成功后标记灵脉已访问，返回地图 |
| 精进道行 | 选择 1 张可升级卡，永久替换为升级版 | 当前牌组至少 1 张可升级卡 | 无可升级卡时禁用；选卡取消返回灵脉界面 | 成功升级后标记灵脉已访问，返回地图 |
| 疗愈道友 | 后续给同行者治疗，数值待队友系统设计 | 当前存在可治疗同行者 | MVP 单人模式隐藏或禁用；不得误点 | MVP 不触发结算，不消耗灵脉 |
| 法宝触发选项 | 按对应法宝定义，如下一场战斗获得 1 点斗劲、获得随机法宝 | 拥有对应法宝且灵脉未结算 | 未拥有法宝时隐藏或禁用 | 成功后标记灵脉已访问，返回地图 |

### 验证结果
- 本次仅修改 `洪荒之后-main/docs` 下文档，未修改 `洪荒之后-code/`、Godot 场景、C# 代码或 `洪荒之后-art/` 资源。
- 未运行 `dotnet build .\AfterHongHuang.csproj`，因为本次为策划文档规则补齐，没有代码或工程变更。

### 风险与待决策
- 风险：精进道行依赖每张卡的升级版数据；开发部接入时若有卡缺少升级版，需要按“无可升级卡/该卡不可选”处理，不应静默失败。
- 风险：法宝触发选项目前只是规则预留；若用户要求立即实现“获得随机法宝”，需先补法宝池、随机规则和持有状态。
- 待决策：疗愈道友的具体回复数值、队友状态结构、队友死亡/倒地规则，等队友系统进入任务卡后再定。

### 下一步
- UI/UX部定义灵脉界面/overlay 的布局、禁用态、原因提示和返回路径。
- 开发部在 UI/UX 口径明确后实现地图灵脉节点进入、选项结算、返回地图和节点已访问状态。

---

## 2026-07-11 | P1-008 / P1-009 角色选择微调与灵脉节点分派

### 用户需求
- [ ] **角色选择界面布局**：截图中 A 区角色栏整体下移，B 区开始游戏按钮整体下移；C 区统计只保留红心图标、冒号和实际生命值/生命上限数字，例如 `心：80/80`。
- [ ] **灵脉节点**：开始实现地图中的灵脉节点交互，参考同类卡牌构筑游戏的休整节点，但游戏内术语必须替换为《洪荒之后》体系。

### 术语口径
- “火堆 / 休息” → `灵脉节点 / 休养生息`，回复 30% 最大生命。
- “升级一张牌” → `精进道行`，选择一张可升级卡并替换为升级版。
- “给队友回血” → `疗愈道友`，当前单人无队友时禁用或隐藏，不做假效果。
- “遗物” → `法宝` 或 `道器`；本轮优先用 `法宝` 表述。
- “获得 1 力量” → `获得 1 点斗劲` 或等价洪荒化表述，具体持续范围由策划部确认后再实现。

### 分派
- UI/UX部：补充角色选择布局口径和灵脉节点界面交互，明确 A/B/C 区位置、C 区简化文本、灵脉选项可用/禁用/隐藏规则。
- 策划部：补充灵脉节点玩法规则，确认休养生息、精进道行、疗愈道友、法宝触发选项的 MVP 范围和术语。
- 开发部：在策划与 UI/UX 口径明确后实现，不直接引入大规模法宝/队友系统；完成后运行 `dotnet build .\AfterHongHuang.csproj` 并更新 `洪荒之后-code/docs/开发记录-code.md`。

### 管理文档
- `任务看板.md`：新增 P1-008、P1-009 并进入 Doing。
- `验收清单.md`：新增 MVP-024、UI-022、UI-023、SYS-007~SYS-009。
- `风险记录.md`：新增 R-021，限制灵脉节点范围扩张。

---

## 2026-07-10 | BUG-20260710-002 手牌默认遮挡层级修复回收

### 部门结果
- [x] **UI/UX部口径**：已将 `06-UI交互/01-MVP界面交互.md` 更新到 v2.2.1，明确 `DefaultZIndexMode = LeftToRightAscending`，默认静止态最左卡在最底层，右侧卡依次覆盖左侧卡。
- [x] **开发部实现**：已将静止态 ZIndex 调整为 `BaseZIndex + index * RestZIndexStep`，默认 `RestZIndexStep = 2`；hover / selected / held 仍分别使用更高层级，保证当前操作卡置顶。
- [x] **验证**：开发部回报 `dotnet build .\AfterHongHuang.csproj` 0 错误 0 警告；Godot headless 启动成功；Debug 启动自查覆盖 1/2/3/5/7/10 张手牌的 ZIndex 递增与交互态置顶。

### 当前状态
- `BUG-20260710-002` 从 Doing 调整为 Review。
- 由于 headless 不能证明真实视觉遮挡观感，仍需用户窗口实机确认默认静止态是否为“左侧在下、右侧压左侧”。

---

## 2026-07-10 | UI/UX部 BUG-20260710-002 手牌遮挡层级口径同步

### 完成事项
- [x] **明确默认遮挡顺序**：在 `06-UI交互/01-MVP界面交互.md` 的 P1-007 章节补充 `DefaultZIndexMode = LeftToRightAscending`，默认静止态最左卡在最底层，右侧卡依次轻微盖住左侧卡。
- [x] **澄清“中心卡最高最正”含义**：该描述只代表几何位置和旋转，不代表中心卡 ZIndex 最高。
- [x] **保留交互态层级提升**：hover / selected / held 状态仍需临时提高层级，当前操作卡压过所有静止手牌，保证可读和可点。
- [x] **补充 Debug 验收点**：Debug overlay 增加每张牌 ZIndex 标签，用于检查默认层级是否左低右高。

### 验收清单同步
- `验收清单.md` 的 UI-021 已由总项目经理登记默认静止态遮挡顺序，本次未覆盖测试结论或任务状态。

### 验证结果
- 本次仅修改 `洪荒之后-main/docs` 下 UI/UX 口径与开发记录，未修改 `洪荒之后-code/`、Godot 场景或美术资源。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---

## 2026-07-10 | UI/UX部 P1-007A 手牌布局调参口径

### 完成事项
- [x] **定义调参项**：在 `06-UI交互/01-MVP界面交互.md` 新增 P1-007 手牌布局参数化章节，明确 `CardSize`、`HandAreaRect`、`HandAnchor`、`VirtualFanCenter`、`CountAngleCurve`、hover/selected、箭头起点与弧高等参数。
- [x] **给出 1920×1080 默认参考值**：以当前约 `180×250px` 卡牌为基准，给出推荐默认值和可调范围，作为开发部后续 `HandLayoutProfile` 或等价配置资源的初始 profile。
- [x] **定义 Debug 可视化点位**：明确虚拟圆心、手牌区域、每张牌目标点/目标框、hover/held 点、箭头起点/控制点/目标点、重排前残影等显示规则。
- [x] **补齐多手牌数验收**：明确 1、2、3、5、7、10 张手牌的布局验收口径，特别约束低张数不留大空洞、高张数不出屏。
- [x] **同步验收清单**：在 `验收清单.md` 新增 UI-019、UI-020、UI-021，用于 P1-007 参数化、Debug 点位和不同手牌数布局验收。

### 给开发部的实现约束
- 先做轻量方案：配置资源或 Inspector 可调 profile + 独立布局计算器 + Debug overlay，不做完整 Godot EditorPlugin。
- 不修改玩法逻辑、卡牌效果、敌人逻辑、奖励逻辑或 P0 已通过交互。
- Debug overlay 默认关闭；正式体验不得显示调参点位。
- 若继续使用现有动态 UI，需把布局计算、参数读取和 Debug 绘制拆出独立方法/类，避免扩大大型动态 UI 系统。

### 验证结果
- 本次仅修改 `洪荒之后-main/docs` 下 UI/UX 口径、验收项和开发记录，未修改 `洪荒之后-code/`、Godot 场景或美术资源。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---

## 2026-07-10 | P1-007B 手牌布局参数化实现回收

### 部门结果
- [x] **UI/UX部口径**：已在 `06-UI交互/01-MVP界面交互.md` v2.2.0 补齐参数表、Debug 点位和 1/2/3/5/7/10 张验收口径。
- [x] **开发部实现**：已新增 `HandLayoutProfile`、`HandLayoutCalculator`、`HandLayoutDebugOverlay`，并接入 `BattleController`。
- [x] **验证**：开发部回报 `dotnet build .\AfterHongHuang.csproj` 0 错误 0 警告；Godot headless 启动成功；Debug 构建启动自查覆盖 1/2/3/5/7/10 张布局计算。

### 当前状态
- `P1-007` 从 Doing 调整为 Review。
- `UI-019` / `UI-020` / `UI-021` 仍保持待测，等待用户窗口实机确认真实遮挡、hover 可读性、Debug 点位观感和调参便利性。

### 用户复测提示
- 默认 Debug 关闭。
- 进入战斗后可按 `F9` 临时开关 Debug overlay。
- 重点看：牌扇是否更自然，1/2/3/5/7/10 张是否围绕中心，hover 是否可读，出牌后是否仍立即重排，Debug 点位是否有助于调参。

---

## 2026-07-10 | BUG-20260710-002 手牌默认遮挡层级错误

### 用户反馈
- [ ] **手牌层级不符合预期**：当前默认静止态是中间卡在最上层；用户期望最左边卡在最底下，每个右边的卡轻微盖住左边的卡。

### 判断
- 这是 P1-007 实机调参反馈，不是玩法逻辑问题。
- 几何位置的“中心卡最高最正”不等于 ZIndex 最高；默认静止态应采用从左到右递增的遮挡层级。
- hover / selected / held 状态仍应临时提升层级，确保当前操作卡可读、可点。

### 分派
- UI/UX部：同步口径，明确默认静止态遮挡顺序为左低右高，避免“中心最高”被误解为层级最高。
- 开发部：修正 `HandLayoutCalculator` / `HandLayoutProfile` 的默认 ZIndex 计算，保持 hover/selected/held 层级提升不回退。

---

## 2026-07-10 | P1-007 手牌布局参数化与调试可视化分派

### 背景
- 用户参考其他 Godot 卡牌项目视频，希望手牌牌扇能像工具化调参一样优化，而不是每次靠改代码猜参数。
- 总项目经理判断：当前阶段不直接开发完整 Godot EditorPlugin，先做更适合 demo 的“配置资源 + Debug 可视化点位 + 独立布局计算器”。

### 分派
- UI/UX部：定义手牌布局调参项、默认目标值、不同手牌数量下的验收口径，以及 Debug 点位显示规则。
- 开发部：在 UI/UX 口径明确后，将当前手牌布局硬编码抽出为 `HandLayoutProfile` / 布局计算器 / Debug overlay；不改玩法、卡牌效果、敌人逻辑或奖励逻辑。

### 验收标准
- 手牌布局参数可通过 Godot 配置资源或 Inspector 调整。
- Debug 开关打开后能显示虚拟圆心、每张牌目标点、手牌区域边界等定位点；关闭后正式体验无额外显示。
- 出牌、hover、点击持牌、斗击箭头、默认自身/无目标牌逻辑不回退。
- 修改后需通过 `dotnet build .\AfterHongHuang.csproj`，并更新代码侧开发记录。

---

## 2026-07-10 | BUG-20260710-001 导出包双击即退出

### 用户反馈
- [ ] **导出包不可玩**：用户双击 `AfterHongHuang.exe` 后程序自行关闭，无法进入标题界面。

### 初步判断
- 初次导出只通过了 `AfterHongHuang.exe --headless --quit`，该烟测不足以证明窗口可玩。
- 当前产物目录只有 `AfterHongHuang.exe`、`AfterHongHuang.pck` 和 zip；对 Godot .NET 项目来说，需重点排查 C#/.NET 程序集和运行配置是否正确打入发布包。
- 导出日志曾提示缺少 `AfterHongHuang.sln`，此前被记录为不阻断；现在应视为高优先级嫌疑项。

### 处理决策
- `P0-005` 从 Done 打回 Doing。
- 新增 `BUG-20260710-001`，由构建发布部和开发部共同处理。
- 构建发布部负责重新导出和可见窗口烟测；开发部负责补齐 `.sln` 或其他 C# 导出工程配置阻塞。

### 处理结果
- [x] 已重新导出 Windows 便携包，正式 zip 现包含 `AfterHongHuang.exe`、`AfterHongHuang.pck` 与 `data_AfterHongHuang_windows_x86_64/`。
- [x] 已完成可见窗口烟测：窗口标题为 `AfterHongHuang`，主窗口保持可见，且已生成截图 `洪荒之后-code/build/v0.0.1-alpha/AfterHongHuang-smoke.png`。
- [x] 旧坏包已移入 `洪荒之后-code/build/failed/v0.0.1-alpha/AfterHongHuang-v0.0.1-alpha-windows-broken.zip`。

---

## 2026-07-10 | P0-005 Windows 可玩包导出完成

### 结果
- [x] 已安装 Godot 4.7 stable mono 导出模板，模板目录为 `C:\Users\ASUS\AppData\Roaming\Godot\export_templates\4.7.stable.mono\`。
- [x] 已使用 Godot `4.7.stable.mono.official.5b4e0cb0f` 完成 Windows release 导出。
- [x] 已生成便携包：`洪荒之后-code/build/v0.0.1-alpha/AfterHongHuang.exe`、`AfterHongHuang.pck`、`data_AfterHongHuang_windows_x86_64/` 与 `AfterHongHuang-v0.0.1-alpha-windows.zip`。
- [x] 已完成最小烟测：zip 现包含 `AfterHongHuang.exe`、`AfterHongHuang.pck` 与 `data_AfterHongHuang_windows_x86_64/`；可见窗口启动后标题为 `AfterHongHuang`，并有 `洪荒之后-code/build/v0.0.1-alpha/AfterHongHuang-smoke.png` 截图。

### 记录
- 导出命令：`Godot_v4.7-stable_mono_win64_console.exe --headless --path D:\GameCode\洪荒之后\洪荒之后-code --export-release "Windows Desktop" "build/v0.0.1-alpha/AfterHongHuang.exe"`
- 打包命令：`Compress-Archive -LiteralPath AfterHongHuang.exe, AfterHongHuang.pck, data_AfterHongHuang_windows_x86_64 -DestinationPath AfterHongHuang-v0.0.1-alpha-windows.zip -Force`
- 已知问题：旧坏包已移入 `洪荒之后-code/build/failed/v0.0.1-alpha/AfterHongHuang-v0.0.1-alpha-windows-broken.zip`；当前正式 zip 已包含 Godot .NET 运行目录，不再是仅 exe/pck 的不可玩包。

---

## 2026-07-10 | P0-005 导出流程授权与分派

### 用户决策
- [x] 允许安装 `Godot 4.7 stable mono` 导出模板。
- [x] 允许新增最小 Windows `export_presets.cfg`。
- [x] 首个可玩包采用 `v0.0.1-alpha` 便携 zip 包，不做安装器、签名、自动更新或商店页。
- [x] 按推荐流程执行：构建发布部主责，开发部配合工程导出配置和阻塞修复。

### 分派原则
- 构建发布部负责模板/导出/产物/发布记录/已知问题。
- 开发部负责最小 `export_presets.cfg` 和导出阻塞的工程侧问题。
- 不改玩法、UI、美术、数值或奖励逻辑。

### 当前状态
- `P0-005` 从 Ready 调整为 Doing。
- 已创建 `p0-005-export-monitor` 监控导出流程，完成后删除，避免空转。

---

## 2026-07-10 | P0-012 用户复测收口与下一步

### 用户复测结论
- [x] **手牌重排残留通过**：用户确认 BUG-20260709-004 修复后已无问题。
- [x] **本轮战斗手牌返工收口**：结合上一轮“其他都已 ok”的用户复测结论，P0-012 相关交互问题收口。

### 收口任务
- `P0-012`：Done。
- `BUG-20260709-001`：Done。
- `BUG-20260709-002`：Done。
- `BUG-20260709-003`：Done。
- `BUG-20260709-004`：Done。

### 下一步
- 将 `P0-005 建立可玩包导出流程` 调整为 Ready。
- 按发布流程，应由开发部/构建发布部确认导出模板、导出命令、产物路径、版本号和最小回归结果。
- `P0-009` / `P0-010` 暂不直接标 Done，等待发布前最小回归路径完整过表后统一收口。

---

## 2026-07-10 | BUG-20260709-004 残留修复回收

### 完成事项
- [x] **读取开发部回报**：确认开发部已完成 `BUG-20260709-004` 残留修复。
- [x] **确认修改范围**：仅修改 `洪荒之后-code/scripts/UI/BattleController.cs` 和 `洪荒之后-code/docs/开发记录-code.md`，未改玩法数据、奖励、地图或美术资源。
- [x] **确认实现方向**：开发部定位到 2 张手牌仍沿用旧的 18 度最小牌扇跨度，导致 3->2 后视觉上像保留中间槽位；已新增低张数牌扇角度计算，2 张总跨度改为 9.5 度。
- [x] **确认验证结果**：开发部 `dotnet build .\AfterHongHuang.csproj` 通过，0 错误 / 0 警告；Godot headless 启动成功，`GameManager` Autoload 初始化。
- [x] **更新管理状态**：P0-012 和 BUG-20260709-004 调整为 Review / 待用户窗口复测。

### 残留风险
- headless 不能证明真实窗口内 3->2、2->1、5->4 的视觉间距，仍需用户实机确认。
- 开发部回报 `洪荒之后-code` 的 git worktree 元数据指向旧路径，导致该目录 `git diff` 当前不可用；不影响本次 build/headless，但后续需要开发部或总项目经理择机清理工作区元数据问题。

---

## 2026-07-09 | BUG-20260709-004 残留问题登记与返工分派

### 用户复测结论
- [x] **通过：P0-012 其他项**：用户确认“其他都已 ok”，包括此前的箭头、点击持牌、日志等已无继续反馈。
- [ ] **失败：手牌重排残留**：手牌从 3 张打出 1 张变为 2 张后，中间空挡仍保留，剩余手牌没有立即按当前数量重新并拢居中。

### 处理决策
- P0-012 从 Review 打回 Doing，仅保留 `BUG-20260709-004` 残留阻断。
- 不再要求 UI/UX部补口径；现有验收标准已清楚：任意卡牌打出、移出或消耗后，剩余手牌必须按当前 `Hand` 重新计算牌扇位置，不保留旧视觉槽位。
- 已向开发部下发残留修复任务，重点检查旧卡节点隐藏但未移除、手牌容器子节点未清理、布局索引/动画/选中状态缓存未重置等路径。

### 文档更新
- `任务看板.md`：P0-012 / BUG-20260709-004 调整为 Doing，并登记本轮分派。
- `验收清单.md`：记录用户复测失败项。
- `项目上下文快照.md`：同步当前唯一阻断和 heartbeat 监控状态。

---

## 2026-07-09 | P0-012 三次返工开发回收

### 完成事项
- [x] **回收 UI/UX部口径**：确认 `06-UI交互/01-MVP界面交互.md` 已补齐上凸箭头、点击持牌和出牌后手牌重排的三次返工验收口径。
- [x] **回收开发部实现**：确认开发部已在 `BattleController.cs` 调整箭头控制点、Self/None 点击持牌跟随鼠标、出牌成功后隐藏旧手牌节点并刷新牌扇。
- [x] **确认代码侧记录**：开发部已更新 `洪荒之后-code/docs/开发记录-code.md`，记录 BUG-20260709-002/003/004。
- [x] **更新管理状态**：P0-012、BUG-20260709-002、BUG-20260709-003、BUG-20260709-004 调整为 Review / 待用户手测。

### 开发部验证结果
- `dotnet build .\AfterHongHuang.csproj`：通过，0 警告 / 0 错误。
- Godot 4.7 mono headless 启动：通过，`GameManager` Autoload 初始化。

### 当前结论
- 三次返工已进入可实机复测状态，但不能标记 Done。
- 仍需用户在窗口中确认：箭头是否为上凸曲线、格挡等默认自身牌点击后是否能跟随鼠标并在手牌区外确认打出、出牌后手牌是否不再留空位。

---

## 2026-07-09 | UI/UX部 P0-012 三次返工口径补齐

### 完成事项
- [x] **复核用户二次实机截图**：确认 hover 摆正已通过，当前剩余问题集中在箭头曲线方向、默认自身/无目标牌点击持牌状态、出牌后手牌重排。
- [x] **补齐上凸箭头口径**：`06-UI交互/01-MVP界面交互.md` 明确敌方目标牌箭头必须从卡牌出发，先向战场上方抬起，再落向目标，形成上凸曲线；下凹弧线不通过。
- [x] **补齐点击持牌口径**：默认自身/无目标牌点击选中后应进入与长按拖拽一致的持牌跟随状态，在手牌区外再次点击或释放即自动打出。
- [x] **补齐手牌重排口径**：任意卡牌打出、消耗或离开手牌后，剩余手牌必须立即按新数量重新居中并拢成扇形，不保留已打出卡牌的空位。

### 修改文档
| 文档 | 变更 |
|------|------|
| `06-UI交互/01-MVP界面交互.md` | v2.1.1 → v2.1.2，补齐 P0-012 三次返工交互口径和用户复测口径 |
| `开发记录.md` | 追加 UI/UX部本次口径补齐记录 |

### 验证结果
- 本次仅修改 `洪荒之后-main/docs` 下 UI/UX 口径与开发记录，未修改 `洪荒之后-code/`、Godot 场景或美术资源。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---

## 2026-07-09 | P0-012 用户二次手测反馈登记

### 用户手测结论
- [x] **通过：hover 摆正**：用户确认卡牌 hover/选中放大已摆正。
- [x] **部分通过：默认自身牌可自动打出**：格挡等牌可以自动打出，但点击选中后的持牌逻辑仍不符合预期。
- [ ] **失败：箭头曲线方向**：当前箭头是下凹弧线；用户期望从卡牌出发的上凸曲线指向目标，参考用户蓝线示意。
- [ ] **失败：点击选中默认自身/无目标牌**：点击后应和长按一样进入跟随鼠标的持牌状态，在手牌区外再次点击或释放时自动打出。
- [ ] **失败：出牌后手牌重排**：手牌从 3 张变 2 张后留下空位，剩余手牌没有重新并拢居中成扇形。

### 处理决策
- P0-012 从 Review 打回 Doing，进入三次返工。
- 新增 `BUG-20260709-002`：斗击箭头曲线方向错误。
- 新增 `BUG-20260709-003`：默认自身/无目标牌点击选中后不能拖出确认。
- 新增 `BUG-20260709-004`：出牌后手牌未重新并拢居中。
- `BUG-20260709-001` 暂保持 Review，等待用户后续确认奖励 overlay 选牌后关闭行为。

### 分派要求
- UI/UX部先补充交互口径，明确箭头上凸曲线、点击持牌与长按持牌的一致性、出牌后手牌重排验收。
- 开发部按 UI/UX 口径修复 `BattleController.cs`，完成后运行 `dotnet build .\AfterHongHuang.csproj` 并更新 `洪荒之后-code/docs/开发记录-code.md`。

---

## 2026-07-09 | P0-012 二次返工与 BUG-20260709-001 开发回收

### 完成事项
- [x] **回收 UI/UX部口径**：确认 `06-UI交互/01-MVP界面交互.md` 已补齐目标策略、hover 摆正放大、卡牌起点箭头和默认自身/无目标牌拖出自动打出的验收口径。
- [x] **回收开发部实现**：确认开发部已在 `DataDefs.cs` 引入 `CardTargetMode`，并在 `BattleController.cs` 接入 hover 摆正、卡牌起点箭头、默认自身/无目标拖出自动打出。
- [x] **回收奖励关闭修复**：确认开发部已在 `CardRewardController.cs` 修复选牌后立即关闭 overlay、禁用跳过和二次交互的问题。
- [x] **确认代码侧记录**：开发部已更新 `洪荒之后-code/docs/开发记录-code.md`。
- [x] **更新管理文档**：`任务看板.md`、`验收清单.md`、`风险记录.md` 和 `项目上下文快照.md` 已同步为 Review / 待用户手测。

### 开发部验证结果
- `dotnet build .\AfterHongHuang.csproj`：通过，0 警告 / 0 错误。
- Godot 4.7 mono headless 启动：通过，`GameManager` Autoload 初始化。

### 当前结论
- `TargetMode` 方向已作为当前实现口径落地；后续可扩展友方、右方、全体、区域等目标模式，不需要按牌名硬编码。
- P0-012 和 BUG-20260709-001 进入用户实机复测阶段，暂不标记 Done。
- Godot 窗口操作后续由用户手测，总项目经理负责登记复测结果和继续分派。

---

## 2026-07-09 | UI/UX部 P0-012 二次返工口径补齐

### 完成事项
- [x] **补齐新版目标策略**：在 `06-UI交互/01-MVP界面交互.md` 明确敌方目标牌、默认自身牌、无目标牌和后续其他目标牌的交互策略。
- [x] **纠正术法操作口径**：默认自身或无目标牌不再要求点击自身立绘，拖出手牌区松手即自动打出；后续新增抽牌等无目标效果复用同一策略。
- [x] **明确 hover 视觉**：hover/选中时从牌扇中原位抬升并摆正放大，不保留原牌扇倾斜角度。
- [x] **明确攻击箭头验收**：敌方目标牌必须显示从卡牌指出的可见箭头，并跟随鼠标/目标；攻击牌本体不得飞出手牌栏。
- [x] **同步验收清单**：修正 `MVP-011`、`UI-010` 中“术法点自身”的旧口径，避免与二次返工目标冲突。

### 给开发部的实现口径
- 不按牌名硬编码目标行为；建议在 `CardInfo` 或后续卡牌配置中引入 `TargetMode` 等等价字段。
- MVP 至少支持 `Enemy`、`Self`、`None` 三类目标策略；后续友方、区域、多目标作为扩展，不在本轮扩大实现。
- 默认自身/无目标牌拖出手牌区松手应直接结算；只有需要选择目标的牌才进入目标选择流程。

### 验证结果
- 本次仅修改 `洪荒之后-main/docs` 下 UI/UX 口径与验收文档，未修改 `洪荒之后-code/`、Godot 场景或 C# 代码。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---

## 2026-07-09 | 用户手测 P0-010 回收与二次修复分派

### 用户手测结论
- [x] **通过：底部牌扇**：手牌已呈牌扇形态。
- [x] **通过：攻击牌边界**：攻击牌本体未飞出手牌栏。
- [x] **通过：战斗日志**：日志表现良好，不影响手牌操作。
- [x] **通过：胜利奖励内容**：本轮未再反馈道器碎片占位。
- [ ] **失败：hover 展示**：卡牌放大时仍保留原牌扇倾斜角度，视觉上应摆正放大。
- [ ] **失败：斗击箭头**：用户未看到从卡牌指出的攻击箭头。
- [ ] **失败：术法操作逻辑**：默认自身或无目标术法不应强制点自身；拖出手牌松手应自动打出。后续抽牌等无目标效果也应走同一目标策略。
- [ ] **失败：卡牌奖励关闭**：选择卡牌后 CardReward overlay 未立即关闭，仍能看到奖励选项并点击跳过。

### 处理决策
- Godot 窗口实机验收后续默认由用户手测，总项目经理负责记录结果、拆分任务和回收，不再依赖 Codex 直接操控 Godot 窗口。
- P0-012 打回二次返工：hover 摆正、斗击箭头可见、卡牌目标策略重构。
- 新增 `BUG-20260709-001`：卡牌奖励选牌后 overlay 未立即关闭。
- `BUG-20260707-004` 和 `P0-011` 按本轮用户手测结果收口为 Done。

### 建议实现方向
- 不按牌名硬编码“格挡/抽牌”等行为。
- 在 `CardInfo` 或后续卡牌配置中引入目标策略字段，例如 `TargetMode = Enemy / Self / None`，或等价的 `RequiresTarget` + `DefaultTarget`。
- 默认自身或无目标术法：拖出手牌区松手即打出，点击选中时也可直接结算或给出明确确认反馈。
- 需要敌方目标的牌：保留卡牌起点箭头与目标确认。

### 验证要求
- 开发部完成后运行 `dotnet build .\AfterHongHuang.csproj`。
- 用户继续手测 P0-010 重点项：hover 摆正、箭头可见、术法默认结算、奖励选牌立即关闭。

---

## 2026-07-09 | P0-010 QA 窗口实机验收受阻

### 完成事项
- [x] **读取 QA 回报**：QA质量保证部已执行 P0-010，确认遵守 QA 边界，未修改代码或资源。
- [x] **确认工程状态**：QA 在 `洪荒之后-code/` 执行 `dotnet build .\AfterHongHuang.csproj`，结果 0 警告 / 0 错误。
- [x] **确认启动状态**：QA 使用 Godot 4.7 mono 可见窗口启动项目，窗口标题为 `AfterHongHuang (DEBUG)`。
- [x] **确认阻塞原因**：Computer Use 在读取/激活窗口时返回 app approval 通道不可用，导致无法操作窗口、截图或完成真实鼠标验收。
- [x] **更新项目文档**：`任务看板.md` 将 P0-010 标记为 Blocked；`验收清单.md` 记录 QA 无法完成窗口实机；`风险记录.md` 新增 R-017。

### 当前结论
- P0-012 代码返工没有被 QA 判定失败，但也没有通过实机验收。
- P0-010 的阻塞原因是验收环境，不是开发部实现回归。
- 步骤 10~13、UI-015、UI-016、UI-017、UI-018 仍未获得可见窗口实机通过结论。

### 下一步
- 需要用户手动体验并反馈 P0-010 重点项，或等待 Computer Use app approval 通道可用后重跑 QA。
- P0-005 可玩包导出继续暂缓，直到 P0-010 实机验收通过或用户明确接受当前风险。

---

## 2026-07-09 | 部门命名调整：旧 UI 部门改为 UI/UX

### 完成事项
- [x] **调整部门名称**：将“旧 UI 部门”统一改为“UI/UX部”，强调其职责是界面流程、交互体验、可用性审查和 UI 实现支持，不等同于传统 Web 前端团队。
- [x] **更新项目准则**：同步更新根目录 `AGENTS.md` 的角色边界、任务分派和 BUG 分派口径。
- [x] **更新管理文档**：同步更新 `任务看板.md`、`验收清单.md`、`风险记录.md`、`决策记录.md`、`版本计划.md` 和 UI 交互文档中的部门归属表述。
- [x] **更新本地工作流 skill**：同步更新 `honghuang-department-workflow` skill，避免后续重新加载技能时回到旧部门名。
- [x] **建立上下文恢复入口**：新增 `项目上下文快照.md`，并在 `AGENTS.md` 中要求压缩、切线或长任务恢复后先读快照。
- [x] **更新文档索引**：`INDEX.md` 已加入 `项目上下文快照.md`。

### 验证结果
- 已检索确认项目文档和本地工作流 skill 中不再保留旧称“旧 UI 部门”。
- 本次仅修改项目管理与工作流文档，未修改 Godot 工程、C# 代码或美术资源。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---

## 2026-07-09 | P0-012 返工回收与 QA 验收分派

### 完成事项
- [x] **回收开发部结果**：确认开发部已完成 P0-012 手牌交互返工，修改 `洪荒之后-code/scripts/UI/BattleController.cs`，并更新 `洪荒之后-code/docs/开发记录-code.md`。
- [x] **确认验证结果**：开发部执行 `dotnet build .\AfterHongHuang.csproj`，结果 0 错误 / 0 警告；Godot 4.7 mono headless 启动成功并初始化 `GameManager`。
- [x] **回收UI/UX部复核**：确认UI/UX部已按用户截图和新口径复核 P0-012，验收重点为虚拟圆心牌扇、hover 原位放大、斗击箭头从卡牌指出、攻击牌本体不飞出手牌栏、自身术法可拖出确认。
- [x] **更新管理文档**：`任务看板.md` 将 P0-012 调整为 Review，P0-010 调整为 QA 独立窗口实机补测；`验收清单.md` 和 `风险记录.md` 同步返工状态与残留风险。
- [x] **下发 QA 验收任务**：已向 QA质量保证部下发 P0-010，要求可见窗口实机验收步骤 10~13、UI-015~UI-018、日志遮挡和胜利奖励内容。

### 当前结论
- P0-012 代码返工已进入可验收状态，但尚不能标记 Done。
- 仍缺玩家可见窗口下的鼠标 hover、点击、拖拽、目标释放、日志鼠标拦截边界和真实手感验证。
- 总项目经理本轮只做回收、记录和分派，未直接修改 Godot 工程或 C# 实现。

### 下一步
- QA质量保证部执行 P0-010 独立窗口实机验收。
- 若 QA 通过，总项目经理收口 P0-009 / P0-010 / BUG-20260707-004 / P0-011 / P0-012，并准备启动 P0-005 可玩包导出流程。
- 若 QA 发现阻断项，按复现步骤分派开发部或UI/UX部修复。

---

## 2026-07-07 | BUG-20260707-004 / P0-011 / P0-012 实现回收

### 完成事项
- [x] **读取UI/UX部交互口径输出**：确认 `06-UI交互/01-MVP界面交互.md` 已补齐日志按钮、弧形手牌、点击/拖拽出牌、胜利奖励不显示道器占位等验收口径。
- [x] **读取开发部实现输出**：确认开发部已在 `BattleController.cs` 实现日志开关与固定高度日志面板、移除胜利弹窗道器碎片占位、手牌弧形布局、点击选中和长按拖拽出牌。
- [x] **读取验证结果**：开发部执行 `dotnet build .\AfterHongHuang.csproj`，结果 0 错误 / 0 警告；Godot 4.7 mono headless 启动成功并初始化 `GameManager`。
- [x] **更新任务状态**：`任务看板.md` 中 BUG-20260707-004 / P0-011 / P0-012 进入 Review；P0-010 从 Blocked 解锁为 Ready。
- [x] **同步验收与风险**：`验收清单.md` 增加开发部实现回收记录；`风险记录.md` 将 R-014 / R-015 调整为“已缓解，待实机验收”。

### 当前结论
- BUG-20260707-004：代码已实现，待窗口实机确认日志默认/展开状态均不遮挡手牌、不拦截操作。
- P0-011：代码已实现，待窗口实机确认胜利弹窗只显示灵韵、卡牌奖励、继续和符修遗箓额外奖励。
- P0-012：代码已实现，待窗口实机确认弧形手牌、点击选中、攻击箭头、术法目标反馈、拖拽合法目标释放和非法回弹手感。

### 下一步
- UI/UX部执行 P0-010 新版窗口实机补测。
- 若 P0-010 通过，总项目经理将 P0-009 / P0-010 及本组三项 Review 任务收口，并准备启动 P0-005 可玩包导出流程。
- 若 P0-010 发现阻断项，再按明确复现步骤分派开发部修复。

### 残留风险
- 目前只有 build 和 headless 启动验证，尚无窗口鼠标拖拽实机结果。
- CardReward 跳过与奖励加入位置不一致仍归 P1-005，不在本轮处理。

---

## 2026-07-04 | 世界观框架确立

### 完成事项
- [x] **确定时间切片**：巫妖量劫落幕千年之后
- [x] **确定游戏类型**：卡牌构筑 Roguelike（参照杀戮尖塔2骨架）
- [x] **世界观调研**：
  - 巫妖量劫始末（帝俊太一陨落、十二祖巫覆灭、不周山倒、洪荒破碎）
  - 战后世界格局（四大部洲、第二代天庭、三教分立）
  - 存活人物梳理（六圣、亚圣、大巫、妖圣）
  - 势力版图（三教、西方教、血海阿修罗、截教万仙）
  - 种族分布（人族崛起、妖族衰落、巫族残存）
- [x] **战力金字塔**：从凡人到天道级的九层体系，标注了每个境界在游戏中的对应层级
- [x] **法宝体系设计**：
  - 六等法宝等级（混沌至宝→先天至宝→先天灵宝→后天灵宝）
  - 游戏中四档稀有度（神话/传说/稀有/普通）
  - 列出40+件具体法宝及游戏效果方向
  - 三大差异化设计原则（持有者关联、残缺主题、法宝反噬）
- [x] **三关+最终Boss**关卡结构草案
- [x] **五个角色**初步设定

### 游戏设定文档版本
`docs/游戏设定文档.md` → v0.2.0

### 待定事项
- [ ] 目标平台
- [ ] 项目目录结构搭建（scripts/scenes/assets）
- [ ] 卡牌系统详细设计（各角色卡池、关键词机制）
- [ ] 战斗系统数学模型（伤害公式、属性系统）
- [ ] 地图/路径选择机制（类似杀戮尖塔的地图选择）

### 技术选型记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 开发语言 | C# | AI Agent 训练数据丰富，静态类型安全性高 |
| 物理引擎 | Jolt Physics | Godot 4.x 推荐，性能优于内置 GodotPhysics |
| 渲染器 | Forward Plus | 默认推荐，平衡画质与性能 |
| 渲染驱动 | D3D12 (Win) | Windows 平台最佳性能 |

---

## 2026-07-05 | 玩法系统深化 + 美术设定启动

### 完成事项
- [x] **道痕系统设计**：
  - 对标杀戮尖塔"首领遗物"机制，独立于常规法宝体系
  - Boss战后3选1固化道韵碎片，每关道痕池独立
  - Act1~3道痕池完整设计（共24件示例道痕），含帝江裂隙、烛九阴凝视等法则级道痕
  - 世界观逻辑：区域中陨落大能的道韵碎片被Boss压制→击败后短暂浮现→时空印记共鸣固化
- [x] **法宝体系简化**：游戏内法宝从四档（神话/传说/稀有/普通）简化为三档（先天至宝/先天灵宝/后天灵宝），与洪荒原著品阶体系一致
- [x] **巫祝美术设定**：完整角色形象设计（性别/体型/面部/发型/肤色与血脉印记/着装分层/武器形态/色板/画风方向/战斗视觉变化），战斗前的静止表情和战斗的状态切换
- [x] **美术音频总览**：确立水墨厚涂风格定位、整体色调方向（废墟棕灰+高饱和点缀）、BGM/音效方向、卡牌美术框架
- [x] **时空残响初始法宝方案**：开局进入遗荒边界，时空印记激活→3选1法宝残影（对标涅奥祝福），可加入"不要法宝"换生命上限变体
- [x] **术语更新**：护盾→护体、消耗→消弭、易伤→易损、力量→斗劲，已全部写入卡牌系统文档
- [x] **文档体系更新**：新增4份文档，INDEX更新至v0.7.0，总文档42份

### 新增文档
| 文档 | 状态 |
|------|:--:|
| `04-美术音频/00-美术音频总览.md` | ✅ |
| `04-美术音频/01-巫祝美术设定.md` | ✅ |
| `03-玩法系统/10-道痕系统.md` | ✅ |

### 更新文档
| 文档 | 变更 |
|------|------|
| `03-玩法系统/04-法宝系统(游戏内).md` | v0.4.0→v0.6.0，四档改为三档，新增道痕系统引用 |
| `03-玩法系统/00-玩法总览.md` | 子文档索引新增10-道痕系统 |
| `INDEX.md` | v0.6.0→v0.7.0，更新统计和文档树 |
| `02-角色/01-巫祝.md` | 无变更（本次无背景故事修改，仅新增美术设定到独立文档） |

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| Boss奖励机制 | 道痕系统（独立于法宝） | 对标塔1首领遗物，世界观自洽；不需要引入新NPC |
| 法宝品阶 | 先天至宝/先天灵宝/后天灵宝 三档 | 与洪荒原著体系一致，后天灵宝创作空间无限可撑起遗物数量 |
| 巫祝武器 | 烬骨甲/骨枪（火中伴生） | 保留体术斗击手感，休眠形态为右臂纹身，世界观自洽 |
| 巫祝画风 | 水墨厚涂 | 区别于日系二次元，契合洪荒粗粝废土气质 |

---

## 2026-07-05（续）| Boss池扩展 + 道痕时机调整

### 完成事项
- [x] **Boss池化**：
  - Act 1：1→6个（相柳之骸 / 共工残影 / 不周山灵 / 九婴残躯 / 夸父遗骨 / 散修聚合体）
  - Act 2：1→3个（鲲鹏残魂 / 金乌怨影 / 帝俊残辉）
  - Act 3：1→3个（盘古残响 / 祝融炎冢 / 烛九阴时涡）
  - 每局每关随机抽1个，大幅降低重复率
  - 每个新Boss带有独特战斗机制设计（水位变化、自焚、时间切换等）
- [x] **道痕触发时机修正**：从"击败Boss后"改为"关卡过渡时"
  - 4次触发：开局→Act1 / Act1→Act2 / Act2→Act3 / Act3→Final
  - 道痕来自区域的整体道韵浓度变化，而非具体被击败的Boss
  - 无论碰到哪个Boss，过渡时的道痕池相同
- [x] **文档联动更新**：关卡设计、玩法总览、INDEX 全部同步

### 更新文档
| 文档 | 变更 |
|------|------|
| `03-玩法系统/09-Boss设计.md` | v0.4.0→v0.6.0，1+1+1→6+3+3 Boss池，全Boss含战斗机制简述 |
| `03-玩法系统/08-关卡设计.md` | v0.4.0→v0.5.0，Boss行改为引用Boss设计文档 |
| `03-玩法系统/10-道痕系统.md` | v1.1.0→v1.2.0，触发时机重构，新增Act3→Final过渡池 |
| `03-玩法系统/00-玩法总览.md` | v0.5.0→v0.6.0，旅程结构图重绘含Boss池+道痕时机 |

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| Boss数量 | Act1(6)/Act2(3)/Act3(3) | Act1变数最大（玩家卡组最弱），最高Boss多样性保证新鲜感 |
| 道痕时机 | 关卡过渡时（非Boss后） | 道痕对标首领遗物，首领遗物在塔1中是过关奖励而非打Boss奖励；区域整体道韵浓度变化比"抢Boss尸体"更自洽 |

---

## 2026-07-05（再续）| 道痕三品分级

### 完成事项
- [x] **道痕战力三品体系**：
  - **上品**（21件）：十二祖巫(12) + 帝俊/太一(2) + 盘古概念道痕(3) + 龙汉四始祖彩蛋(4)
  - **中品**（10件）：伏羲 + 鲲鹏恶尸/善尸(2) + 计蒙/英招/白泽(3) + 后羿/夸父/相柳/九凤(4)
  - **下品**（19件）：九大金乌(9) + 妖将飞廉~毕方(8) + 星辰幡执旗手 + 都天神煞残魂
- [x] **池子按品级重组**：
  - 开局→Act1：下品池（19件）——遗荒外围，最弱道韵
  - Act1→Act2：中品池（10件）——大巫/妖圣级
  - Act2→Act3 + Act3→Final：上品池（17件 + 4彩蛋）——祖巫/天帝级
- [x] **新增概念道痕**：盘古遗力、开天余势、十二都天神煞 3件，不以个体逝者形式存在
- [x] **附录A全面更新**：47条→50条，每条标注品级，A.8总览表改为三品统计

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 道痕分级标准 | 逝者生前洪荒战力 | 遗荒同心圆结构（越核心→道韵越强→逝者战力越强），逻辑自洽 |
| 九大金乌 | 下品 | 虽为帝俊之子，但功法不精，被后羿一人射杀九只，战力≈太乙金仙 |
| 夸父 | 中品 | 大巫（≈大罗金仙门槛），逐日是强悍表现，被十金乌联手才死 |
| 盘古相关 | 上品（概念道痕） | 盘古为洪荒第一战力，即便只残留万分之一的意志也是上品 |

---

## 2026-07-05（三续）| 道痕池优化 + 灵脉节点

### 完成事项
- [x] **道痕池多样化重构**（50条→48条）：
  - **合并**：九大金乌 9条→1条"九乌坠日"（怨念聚合体）
  - **升级**：穷奇/毕方/鬼车/钦原 下→中（远古神兽/凶鸟/灾鸟≥大巫级）
  - **新增 6 条**：祭巫血祀(中)、符修残页/体修残躯/阵修碎盘(下×3)、蛟龙骨/灵凤血(下×2)
  - 附录A重组为12个分组（A.1~A.12），A.13总览表
- [x] **池容量调整**：下品池 19→12 / 中品池 10→15 / 上品池不变
- [x] **灵脉节点（火堆）术语确定**：
  - 对标杀戮尖塔"火堆"→洪荒设"灵脉节点"（地脉灵气汇聚处）
  - 休养生息（Rest/回血） / 精进道行（Smith/升级卡牌） / 疗愈道友（Recall/回同伴血）
- [x] **文档联动**：玩法总览新增灵脉节点章节

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 九大金乌 | 合并为1条"九乌坠日" | 分开9条无设计差异，合并后作为"怨念聚合体"反而更有特色 |
| 穷奇/毕方/鬼车/钦原 | 升至中品 | 穷奇(上古四凶)/毕方(山海经神鸟)/鬼车(九头灾鸟)/钦原(蜇人即死)战力≥大巫 |
| 散修三条 | 下品 | 金仙~太乙金仙殒落者，代表"不自量力闯遗荒"的众生相 |
| 蛟龙/灵凤 | 下品 | 龙汉后裔旁支，未入谱籍未成年个体 |
| 火堆命名 | 灵脉节点 | "火堆"在洪荒废墟中不可理喻；灵脉是洪荒特色的固定节点概念 |

---

## 2026-07-05（四续）| 道痕命名归一

### 完成事项
- [x] **道痕命名全面修正**（18条）：去除"捡尸体"语义（骨/颅/角/血/躯/页/羽/弓等），改为**感悟/法则/执念**类命名
  - 含义修正 12 条：飞廉失风 / 九婴水火 / 呲铁锻金 / 后羿贯日 / 相柳毒泽 / 九凤归途 / 祭巫燃血 / 符修遗篆 / 体修金刚 / 阵修困天 / 蛟龙蜕鳞 / 灵凤离火 / 始麒麟镇岳
  - 微调 6 条：奢比尸瘴 / 天吴逐风 / 英招巡天 / 鬼车灾兆 / 钦原绝命 / 伏羲演卦
- [x] 池子表格 + 附录A + 彩蛋全链路同步更新

### 命名原则

| 旧方向 | 新方向 | 例 |
|------|------|------|
| 物理残片（骨/颅/角/血/羽/琴） | 执念/法则/意志 | 后羿落日弓→后羿贯日：不是遗物弓，是贯穿太阳的意志 |
| 生理行为（嚼/泣/蛰） | 气象/法则级现象 | 鬼车泣血→鬼车灾兆：不是滴血，是灾祸的预警 |
| 遗落物品（页/躯/盘/血祀） | 遗落物品 | 未完成或已完成的道法瞬间 | 阵修碎盘→阵修困天：不是碎掉的阵眼，是困住天崩三息的阵道意志 |

---

## 2026-07-05（五续）| 卡牌奖励机制 + 符修遗箓效果

### 完成事项
- [x] **卡牌奖励机制完整设计**（对标杀戮尖塔1/2源码逻辑）：
  - 基础概率：天品5% / 地品25% / 玄品70%
  - rareOffset 动态补正：出玄品+1%，出天品重置-5，上限40%
  - 精英战加成：天品+10%（初始15%，上限50%）
  - Boss战：必定至少1张天品
  - 升级概率：Act1 0% / Act2 12.5% / Act3 25%
  - 3张顺序抽选，互不重复，来自角色卡池
  - 劫数不出现在卡牌奖励中
- [x] **符修遗箓效果**：击败敌人后额外获得一次卡牌奖励（多一次3选1）
- [x] 道痕文档 + 附录A 同步更新符修条目（符修遗篆→符修遗箓）
- [x] 01-卡牌系统.md 新增第5章"卡牌奖励机制"，章节号全部顺延

### 更新文档
| 文档 | 变更 |
|------|------|
| `03-玩法系统/01-卡牌系统.md` | v0.7.0→v0.8.0，新增§5卡牌奖励机制，章节号+1 |
| `03-玩法系统/10-道痕系统.md` | 符修遗箓效果填写，附录A同步 |

---

## 2026-07-05（六续）| MVP验证策划补全

### 完成事项
- [x] **战斗系统文档创建**（`02-战斗系统.md` v1.0.0）：
  - 回合流程：玩家回合→玩家回合结束结算→敌人回合→敌人回合结束结算→循环
  - 核心属性：巫祝80HP、基础灵力3/回合(通用)、无初始护体
  - 护体机制：本回合生效、回合结束清零、可叠加
  - 伤害公式：`(面板伤害 + 斗劲) × 易损(1.5) × 虚弱(0.75)`，向下取整
  - 胜负条件、敌人意图系统
  - 状态效果总览（15项），新增"永炎"术语
- [x] **怪物池体系**（`08-关卡设计.md` v0.5→v0.6）：
  - 弱怪/强怪/精英三池分级
  - 第一大关前3场弱怪，其余大关前2场弱怪
  - 奖励规则：卡牌3选1 + 灵韵
- [x] **第一个小怪**：山野妖猴（弱怪池）HP60，3回合意图循环（强化→打7→打13），灵猴闪避机制
- [x] **道痕效果填充**（`10-道痕系统.md`）：九乌坠日（开局3层永炎）/ 体修金刚（+10最大生命值）/ 符修遗箓（此前已填）
- [x] **初始卡牌升级版**（`01-卡牌系统.md` v0.8→v0.9）：拳袭+ 6→9 / 格挡+ 5→8 / 烈拳+ 易伤多一层 / 燃血+ 扣血2→1
- [x] **术语表补全**：永炎（回合结束扣层数值，不衰减）/ 灵韵（暂定货币名）
- [x] 玩法总览、INDEX同步更新

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| MVP卡牌奖励 | 巫祝奖赏卡池用初始打防凑数 | 先验证流程，后续再设计正式奖赏卡池 |
| 灵猴闪避 | 按斗击命中计数（非伤害） | 鼓励术法牌使用，增加策略性 |
| 永炎机制 | 不衰减的持续扣血 | 对标尖塔2的Burn机制但更强，适合道痕级效果 |
| 灵韵 | 暂定名 | 用户提出，后续可能调整 |

---
## 2026-07-05（七续）| UI交互设计

### 完成事项
- [x] **UI交互独立分类**：新建 `06-UI交互/` 目录，与美术脱钩，专注交互逻辑
- [x] **UI交互总览**（`00-UI交互总览.md` v1.0.0）：
  - 设计原则：直觉优先、反馈即时、信息分层、洪荒气质
  - 操作模型：鼠标拖拽出牌（对标尖塔2）、右键取消、点击选择
  - 分辨率基准：1920×1080 设计基准 / 1280×720 最小支持
  - 界面流转图：A角色选择 → B道痕选择 → C战斗 → D卡牌奖励 → E牌组展示
  - 通用组件规范：按钮4态、Tooltip 0.3s、过渡动画时长、确认/取消模式
  - 字体与色彩规范：书法体标题 + 8色值体系（深棕底/米白字/赤红伤害/青蓝护体/古金稀有/紫韵道痕/墨绿增益/暗红减益）
  - 信息架构优先级：Level 1~4 分层
- [x] **MVP界面交互详设**（`01-MVP界面交互.md` v1.0.0）：
  - 场景A·角色选择：巫祝立绘左侧 + 角色信息右侧 + 开始旅程按钮
  - 场景B·道痕选择：3张横排并排，悬停放大110%，点击即固化不可逆
  - 场景C·战斗画面（对标尖塔2）：
    - 顶部UI栏：灵韵/道痕数量/场景名/牌库弃牌堆计数
    - 左侧玩家区：立绘半身 + 脚下HP/护体条 + 头顶buff图标
    - 右侧敌人区：立绘 + HP/护体条 + 意图图标（剑/升腾/符箓）
    - 底部手牌区：略微扇形展开(±2°)，左上角显示费用（火焰圆点）
    - 出牌交互：拖拽→目标高亮→松手打出→飞入弃牌堆；灵力不足灰暗禁用
    - 伤害飘字：普通白/易损红/护体青蓝/闪避灰
    - 回合结束按钮：脉动光晕提示+悬停放大
  - 场景D·卡牌奖励：3张横排 + 稀有度边框（铜/银/金/紫金）+ 跳过按钮
  - 场景E·牌组展示：网格排列 + 新卡高亮 + 总数统计
  - MVP交互检查清单：9项验收标准

### 新增文档
| 文档 | 状态 |
|------|:--:|
| `06-UI交互/00-UI交互总览.md` | ✅ v1.0 |
| `06-UI交互/01-MVP界面交互.md` | ✅ v1.0 |

### 更新文档
| 文档 | 变更 |
|------|------|
| `INDEX.md` | v0.8→v0.9，新增06-UI交互分类，总文档44份，29完成 |

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| UI交互独立成分类 | 新建06-UI交互/ | 与04-美术音频/脱钩；美术管"长什么样"，交互管"怎么用" |
| 出牌操作方式 | 鼠标拖拽（对标尖塔2） | 用户明确选择；拖拽比点击→选目标更直觉，与尖塔2一致 |
| 选择界面布局 | 横排并排 | 3张卡并排一目了然，扇形展开手牌过多时不适用 |
| 战斗布局 | 对标尖塔2（左右立绘+底部手牌扇形） | 已验证的成熟布局；左右各放立绘比纯抽象图标沉浸感强 |
| 道痕选择确认 | 点击即确认，无二次弹窗 | 避免打断节奏；3选1不是不可逆风险操作 |
| UI交互vs UI美术 | 交互文档先于美术文档 | 先定交互逻辑（开发部可先行搭建UI框架），美术资源后续根据交互文档产出 |

---
## 2026-07-05（八续）| UI交互补充 — 标题界面 + 角色选择升级 + 道痕地图双面板

### 完成事项
- [x] **标题界面设计**（场景0）：
  - 全屏大插画背景 + 4个按钮（开始游戏/时间线/设置/退出）
  - 时间线、设置 MVP 阶段点击弹"开发中"占位提示
- [x] **角色选择界面重设计**（场景A）：
  - 从单人立绘改为**全屏背景插画切换式**：选中角色时背景切换为该角色个人插画
  - 底部角色栏：5 角色头像 + 1 随机按钮，水平并排
  - 头像状态体系：已解锁选中（金框+✓）/ 锁定（灰度+🔒）/ 随机（骰子图标）
  - MVP 仅巫祝可用，后 4 个灰掉但不隐藏——让玩家感知"还有其他角色"
  - 锁定角色悬停显示"此角色将在后续版本中开放"
- [x] **道痕选择 + 地图双面板**（场景B）：
  - 右上角"🗺 地图"按钮切换至地图路线视图
  - 右上角"✦ 道痕"按钮切回道痕选择（按钮上红点提示"未选道痕"）
  - 来回切换不受限制，但选道痕前地图所有战斗节点 🔒 锁定
  - 选完道痕 → 节点解锁 → 点击进入战斗
  - **必须先选道痕才能打怪**：强制流程约束
- [x] **全链路文档同步更新**：
  - 00-UI交互总览 v1.0→v1.1：界面流转图新增场景0 + 场景B双面板标识
  - 01-MVP界面交互 v1.0→v2.0：6 个场景完整重写
  - MVP开发需求 v1.0→v2.0：流程 + 验收标准从 6 项扩展到 12 项
  - 开发记录：八续

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 角色选择界面 | 全屏插画切换式 + 底部头像栏 | 比单人立绘更大气，每个角色有自己的"主题画面"；底部头像栏类似尖塔2角色选择，玩家熟悉 |
| 锁定角色显示 | 灰掉但不隐藏 | 让玩家感知到"还有内容未解锁"，增强期待感和Demo的"未完待续"感受 |
| 道痕+地图双面板 | 右上角按钮切换，非独立场景 | 简化场景管理；尖塔2中道痕选择和地图也是同一界面；切换自由但流程有约束 |
| 道痕→地图→战斗流程 | 选道痕→解锁地图→点节点→战斗 | 符合"出门前先选好道痕"的逻辑；地图视觉上让玩家理解"我在哪" |

---
## 2026-07-05（九续）| UI 重构 — 横向分岔地图 + 持久化TopBar + 返回按钮

### 完成事项
- [x] **角色选择增加返回按钮**：左上角「← 返回」按钮，点击回到标题界面
- [x] **新增 MapScene 横向分岔地图场景**：
  - 横向布局（从左到右流转），对标尖塔2纵向地图但改为横向
  - 两端固定端点：左端=道韵选择节点，右端=关底Boss节点
  - 中间采用树状分岔路线设计（Layer 1 分两条岔路 → Layer 2 汇合 → Layer 3 Boss）
  - 节点按类型着色（紫色=道韵、红色=战斗、绿色=灵脉、深红=Boss）
  - 层间连线使用 ColorRect 绘制
- [x] **TopBar 通用持久化状态栏**：
  - 左侧：角色名、HP x/y、难度标签、灵韵数量
  - 右侧：📋套牌查看、🗺地图、⚙设置按钮
  - HP 颜色随血量比例变化（绿→橙→红）
  - 地图界面禁用地图按钮，战斗界面全部可用
  - 从角色选择进入后即固定存在，战斗中也持续显示
- [x] **道痕选择改为 MapScene 内 overlay 弹窗**：
  - 点击地图左侧"道韵选择"节点→弹出半透明遮罩+3张道痕卡
  - 选择后关闭 overlay，地图节点自动解锁
  - 道痕选择不可逆，无二次确认
- [x] **地图数据结构重构**（DataDefs）：
  - 新增 `MapLayerData`（分层数据）、`MapConnection`（层间连接关系）
  - `MapNodeInfo` 新增 `Layer`、`PositionIndex` 属性
  - `MapNodeType` 枚举变更：道韵/战斗/灵脉/Boss
  - 新增 `Difficulty` 字段到角色信息
- [x] **地图可访问性逻辑**：
  - 道韵节点始终可点击
  - 道痕未选时所有其他节点锁定（灰色不可点击）
  - 已访问节点不可重复点击
  - 仅与当前层有连接关系的下一层节点可访问
  - 通过 `VisitedNodeIds` 追踪访问历史
- [x] **BattleController 全面重构**：
  - 所有子节点改为代码生成（.tscn 精简到仅 root Control）
  - 集成 TopBar 组件
  - 新增套牌查看弹窗（战斗中点击📋套牌按钮）
  - 战斗胜利后返回 MapScene（非 DeckDisplay）
- [x] **CardReward 流程修正**：奖励选完后返回地图继续探索
- [x] **DaoMarkSelectController 精简**：移除地图面板代码，仅保留纯道痕选择逻辑
- [x] **编译验证**：`dotnet build` → 0 错误 0 警告

### 新增文件
| 文件 | 说明 |
|------|------|
| `scripts/UI/TopBar.cs` | 通用持久化顶部状态栏组件 |
| `scripts/UI/MapController.cs` | 横向分岔地图场景控制器（含道痕 overlay） |
| `scenes/Map/Map.tscn` | 地图场景（最小化，UI 全部代码生成） |

### 修改文件
| 文件 | 变更 |
|------|------|
| `scripts/Data/DataDefs.cs` | 地图分层数据结构、连接关系、角色难度、新枚举 |
| `scripts/Core/GameManager.cs` | 新增 Difficulty/VisitedNodeIds/CurrentMapLayer/CurrentMapIndex |
| `scripts/UI/CharacterSelectController.cs` | 新增返回按钮 + 跳转目标改为 MapScene |
| `scripts/UI/BattleController.cs` | 全面重构为代码生成UI + 集成TopBar |
| `scripts/UI/CardRewardController.cs` | 奖励后跳转到 MapScene |
| `scripts/UI/DaoMarkSelectController.cs` | 移除地图面板代码 |
| `scenes/CharacterSelect/CharacterSelect.tscn` | 新增返回按钮节点 |
| `scenes/Battle/Battle.tscn` | 精简到仅 root Control |

### 场景流转（新）

```
Title → CharacterSelect → Map → (道痕 overlay) → Map → Battle → CardReward → Map → ... → Boss → 结束
                                ↑_____________________________↲
```

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 地图布局方向 | 横向（左→右） | 用户明确要求；横向布局更符合"从边缘向核心收束"的叙事方向 |
| 地图两端固定端点 | 左=道韵选择，右=关底Boss | 道韵是"出发前的准备"，Boss 是"关底目标"，两端端点给玩家清晰的方向感 |
| TopBar 实现方式 | 代码生成（Control + 子节点） | 避免 .tscn 依赖，方便在 Map/Battle 中复用 |
| 道痕选择交互 | 地图内 overlay 弹窗 | 避免场景切换打断沉浸感，符合"先选道痕再看地图"的流程约束 |
| 地图节点可访问性 | 仅连接+未访问节点可点击 | 防止跳层和重复，保证路线选择有意义 |
| 战斗后返回 | 返回 MapScene | 支持连续探索多个节点，非一次性战斗 |
| TopBar 地图按钮 | 地图界面禁用，战斗界面可用 | 地图界面不需要"去地图"；战斗中"去地图"可作为逃跑/查看功能（MVP暂弹开发中）|

---
## 2026-07-05（十续）| 分辨率调整 + 设置菜单 + 战斗UI重构

### 完成事项
- [x] **默认分辨率**：project.godot 设为 1920×1080，解除之前未设置的"小窗口"问题
- [x] **设置菜单补全**：
  - 新增 `SettingsHelper.cs` 静态共享工具类，Map/Battle 共用同一套设置弹窗
  - 分辨率切换：1920×1080（推荐）/ 1600×900 / 1280×720，实时生效
  - 返回标题界面按钮
  - 退出游戏按钮
- [x] **战斗UI重构**（对标尖塔2）：
  - 左侧：玩家立绘占位（280×420暗红色块 + 角色名） + HP条/护体条 + 灵力/斗劲 + Buff状态
  - 右侧：敌人立绘占位（280×420暗色块 + 怪物名） + 意图图标（立绘上方） + HP条 + Buff状态
  - 底部：手牌区域（半透明横条背景 + 可拖拽卡牌按钮）
  - 卡牌拖拽打出：按住左键拖拽 → 释放到敌人区或玩家区 → 打出；释放到无效区域 → 回弹；右键取消拖拽
  - 日志面板缩小到顶上居中一行（不占大片空间）
- [x] **新增设置菜单文档**：`-main/docs/06-UI交互/06-设置菜单.md`，记录菜单结构和待扩展项
- [x] **存档设计决策**：永久采用节点级存档（存档时机在节点过渡之间，战斗中不存档）。S/L 是这类游戏的核心战术玩法之一，节点级存档天然支持

### 新增文件
| 文件 | 说明 |
|------|------|
| `scripts/UI/SettingsHelper.cs` | 设置弹窗静态工具类（分辨率+返回+退出） |
| `docs/06-UI交互/06-设置菜单.md` | 设置菜单内容文档 |

### 修改文件
| 文件 | 变更 |
|------|------|
| `project.godot` | 新增 viewport_width=1920 / viewport_height=1080 |
| `scripts/UI/BattleController.cs` | 全面重写：左右立绘布局 + 拖拽卡牌 + CardDragButton 子类 |
| `scripts/UI/MapController.cs` | 设置按钮改用 SettingsHelper；尺寸适配 1920；移除旧的 ShowSettingsDialog |
| `scripts/UI/TopBar.cs` | CustomMinimumSize 适配 1920 |

### 战斗UI布局（1920×1080）

```
┌─────────────────────────────────────────────────────────┐
│  [⚙巫祝 HP:80/80 地仙 灵韵:0]      [📋套牌][🗺地图][⚙] │  ← TopBar 44px
├──────┬────────────────────────┬────────────────┬────────┤
│      │     [战斗日志 一行]     │                │        │
│  ┌───┐│                        │          ┌───┐ │        │
│  │巫祝││     地图背景占位       │          │妖猴│ │        │
│  │  ││                        │          │  │ │        │
│  └───┘│                        │          └───┘ │        │
│ 生命   │ 80/80                   │ 意图：⚔ 攻击 8  │        │
│ 护体   │ 0                       │ HP 60/60         │        │
│ 灵力 3/3  斗劲 0                │ 状态：无           │        │
│ 状态：无  │                        │                  │        │
├──────┴────────────────────────┴────────────────┴────────┤
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐              [结束回合]  │
│  │拳袭│ │格挡│ │烈拳│ │燃血│              你的回合     │
│  └────┘ └────┘ └────┘ └────┘                           │  ← 手牌区 280px
└─────────────────────────────────────────────────────────┘
```

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 设置弹窗实现 | 静态工具类 SettingsHelper | Map/Battle 共用，避免重复代码 |
| 战斗UI布局 | 左右立绘对立 + 中立绘中间底部手牌 | 对标尖塔2，立绘占位方便后续替换美术资源 |
| 卡牌打出 | 拖拽释放（Drag & Drop） | 比点击更有"打出牌"的代入感；右键/无效区取消回弹 |
| 套牌按钮 | 战斗中可用（弹窗查看） | MVP 阶段维持原功能 |
| 地图按钮 | 战斗中暂不可用 | 当前实现为基础弹窗占位，后续改为呼出半屏地图 |
| 设置菜单文档 | 独立文档 `06-设置菜单.md` | 便于团队对齐设置项范围和扩展计划 |

---
## 2026-07-05（十一续）| 道韵 overlay 改进 + UI 等比缩放

### 完成事项
- [x] **道韵 overlay 可切换回地图**：overlay 右上角新增「返回地图」按钮，关闭后地图可继续交互
- [x] **已选道韵再次点击**：若道韵已固化，再次点击道韵节点显示"查看已选道韵"视图——选中卡片高亮 + 标题金色 + "✓ 已固化"标记，其余两张变灰 35% 透明度
- [x] **UI 等比缩放**：`project.godot` stretch/mode 从 `canvas_items` 改为 `viewport` + 设定窗口覆盖尺寸 1920×1080
  - `viewport` 模式：Godot 将整个视口渲染到 1920×1080 设计分辨率，然后等比缩放到物理窗口
  - 代码中所有硬编码坐标（SetPosition、CustomMinimumSize 等）无需逐行修改——Godot 自动处理缩放
  - 分辨率切换（设置菜单）仍正常工作，窗口 resize 后画面自动等比适配，无黑边无扭曲

### 修改文件
| 文件 | 变更 |
|------|------|
| `project.godot` | stretch/mode=viewport + window_width/height_override=1920×1080 |
| `scripts/UI/MapController.cs` | 重写 ShowDaoMarkOverlay：双模式（选择/查看）+ 返回按钮 + CloseDaoMarkOverlay 方法 |

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| UI 缩放方案 | stretch/mode=viewport | 比 canvas_items 更强——连代码生成的 Control 节点都会自动缩放；所有动画/字体/间距等比适配，无需逐行修改坐标 |
| 道韵查看模式 | 重开 overlay，选中高亮+其余变灰 | 比新开场景更轻量；保持地图为"主体画面"的设计一致性 |

---
## 2026-07-05（十二续）| 卡牌交互改版 + 战斗胜利弹窗

### 完成事项
- [x] **卡牌交互改造（对标尖塔2）**：
  - 取消拖拽到目标身体的交互，改为：**点击卡牌→上浮高亮（Y-35px + 金边变色）→ 点击目标区域确认打出**
  - 斗击卡：选中后从角色立绘中心画弧形箭头指向鼠标，提示"需要选择目标"
  - 术法卡（自施法）：选中后提示「点击自身立绘区域确认」
  - 取消方式：右键 / 点空白 / 再点同一张卡
  - 移除旧的 `CardDragButton` 类，新增 `CardButton` 类（仅处理选中/上浮视觉）
  - 箭头绘制：BattleController 用 `_Draw()` + 二次贝塞尔曲线 + 三角形箭头头
- [x] **战斗胜利弹窗（就地弹出，不切场景）**：
  - 战斗胜利后不立即跳转 CardReward 场景，而是在当前战斗页面上弹出半屏弹窗（960×760，居中）
  - 弹窗内容分多行展示：
    1. 「获得灵韵 +12」— 自动领取
    2. 「道器碎片 ×1」（暂未开放，灰掉）— 计划中类似尖塔2药水系统
    3. 「卡牌奖励（3选1）」— 点击后跳转 CardReward 场景
    4. 「返回地图」— 跳过卡牌奖励直接回地图（灵韵照给）
  - 灵韵暂存机制：战斗结算不立即加灵韵，等弹窗关闭/领取时才入账（`UnclaimedLingYun`）
- [x] **CardInfo 新增属性**：`TargetsEnemy` 计算属性（斗击=true，术法=false），用于判断是否需要目标箭头

### 新增/变更文件
| 文件 | 变更 |
|------|------|
| `scripts/UI/BattleController.cs` | 全面重写：CardButton 选中交互 + _Draw 弧形箭头 + ShowVictoryPopup 结算弹窗 |
| `scripts/Data/DataDefs.cs` | CardInfo 新增 `TargetsEnemy` 属性 |
| `scripts/Core/GameManager.cs` | 新增 `UnclaimedLingYun` 暂存字段 |

### 卡牌交互流程（新）

```
点击卡牌 → 上浮高亮
  ├─ 斗击卡 → 显示弧形箭头指向鼠标 → 点击敌人立绘区 → 打出
  └─ 术法卡 → 日志提示"点击自身立绘确认" → 点击玩家立绘区 → 打出

取消 → 右键 / 点空白 / 再点同一张卡
```

### 战斗胜利弹窗布局

```
                 ┌─────────────────────────┐
                 │      — 胜 利 —          │
                 │                         │
                 │   获得灵韵 +12          │
                 │                         │
                 │  [道器碎片×1（暂未开放）] │  ← 灰掉
                 │                         │
                 │  [卡牌奖励（3选1）]      │
                 │                         │
                 │  [返回地图]             │
                 │  （卡牌奖励不领视为放弃）  │
                 └─────────────────────────┘
```

---
## 2026-07-05（十三续）| 地图统一呼出式 + 卷轴动画

### 完成事项
- [x] **地图改为统一呼出式 overlay**（不再作为默认全屏主体）：
  - 进地图场景后，底层显示"当前节点内容"（未选道韵→道韵选择 overlay；已选→提示信息）
  - 地图通过 TopBar 地图按钮 toggle 呼出/关闭
  - 战斗中也是 toggle 呼出式，再点/点收卷关闭
- [x] **卷轴从右向左展开动画**：
  - overlay 初始位置 x=1920 width=0 → 动画展开到 x=320 width=1600
  - ease-in-out 缓动，展开 350ms，收起 250ms
  - 古纸棕黄卷轴色背景，标题 + 收卷按钮
- [x] **MapRenderer 提取共享绘制**：战斗中只读地图使用 MapRenderer.Draw()
- [x] **MapController 全面重写**：
  - 旧代码将所有 UI 铺在固定地图上（~500 行）
  - 新代码底层=道韵选择/节点信息，地图=呼出式 overlay（含交互按钮）
  - 节点样式用颜色区分（黄=当前位置，灰=已访问，紫=道韵，红=Boss，绿=灵脉）
- [x] **战斗胜利弹窗清理**：返回地图 → 继续按钮（等效地图跳转）；去掉"不领视为放弃"文字

### 新增文件
| 文件 | 说明 |
|------|------|
| `scripts/UI/MapRenderer.cs` | 地图只读绘制工具类（战斗 overlay 用） |

### 修改文件
| 文件 | 变更 |
|------|------|
| `scripts/UI/MapController.cs` | 全面重写：底层节点内容 + 呼出地图 overlay + 卷轴动画 + toggle |
| `scripts/UI/BattleController.cs` | 地图 overlay 用 MapRenderer + 卷轴动画 + toggle |

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 地图位置 | 统一呼出式 overlay | 用户明确要求；底层=当前节点，地图=可随时开关的呼出界面 |
| 呼出动画 | 右→左卷轴展开 | 符合"卷轴"意境，古纸色调增强沉浸感 |
| 动画实现 | 帧等待 + ease-in-out | 避免 Tween 依赖，GDScript/C# 兼容一致 |
| MapRenderer | 静态工具类 | Battle 和 Map 各取所需（Battle 用只读渲染，Map 用交互按钮） |

---
## 2026-07-05（十四续）| 道韵底页化 + 战斗地图 toggle + 弹窗优化

### 完成事项
- [x] **道韵选择迁移到底页**：
  - 旧：道韵选择是 overlay（叠在空洞的"道韵浮现"页面上方）
  - 新：道韵选择直接渲染在底页（`_mainArea` 中），无叠加层
  - 道韵未选 → 底页直接显示 3 张道韵卡
  - 点「固化」→ 原地切换（选中高亮 + ✓ 标记，其余变灰 35%），无需销毁重建
  - 选完后进入下一节点 → 底页改为节点信息（名称、类型、道痕数量）
- [x] **战斗 TopBar 地图按钮修复**：
  - 旧：战斗中地图按钮跳转 MapScene → 战斗场景被销毁，回不去
  - 新：战斗中地图按钮 → toggle 呼出只读地图 overlay（卷轴动画），再点关闭回到战斗
- [x] **CardReward 改为静态 overlay 工具类**：
  - 旧：独立 .tscn 场景，GoToScene 跳转 → 背景为空
  - 新：`CardRewardHelper.Show()` 在当前页面上叠全屏遮罩 + 3选1卡牌
- [x] **胜利弹窗简化**：
  - 去掉「返回地图」按钮和「不领视为放弃」提示
  - 改为「继续」按钮 → 领灵韵后回 MapScene
  - 卡牌奖励/继续行为等效 TopBar 地图按钮
- [x] **回合结束弃牌**：`EndPlayerTurn()` 新增 `Hand → DiscardPile` 清空
- [x] **文档更新**：-code 和 -main 开发记录同步补全（十四续 + 续6）

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 道韵放位置 | 底页直接渲染 | 用户反馈 overlay 叠层不干净；选完立即原地看到结果更流畅 |
| 道韵切换方式 | 原地改 UI（不销毁重建） | 无闪烁、代码从树查找改起而非全量重建 |
| 战斗中地图 | toggle 呼出式（不切场景） | 场景切换会销毁战斗状态；呼出式 overlay 可随时开关 |
| CardReward | 静态工具类 overlay | 不依赖 .tscn 文件，无场景跳转，统一 UI 代码生成风格 |

---
## 2026-07-06（十五续）| CardReward 修复 + 文档补全

### 完成事项
- [x] **CardReward 跳过按钮移除**：对标尖塔2，跳过不是独立按钮——不选直接关 overlay 走下一节点即为跳过
- [x] **CardReward overlay 遮罩修正**：overlay 从 FullRect 锚点改为手动 SetPosition(0,44) + Size(1920,1036)，遮罩从 TopBar 下方开始，顶部功能栏可正常交互
- [x] **文档补全**：
  - `03-玩法系统/03-地图与路径.md` 从占位填成 v1.0.0 正式文档（节点类型/数据结构/访问判定/交互/可视化/后续规划，共 8 章）
  - `03-玩法系统/01-卡牌系统.md` 新增 MVP 阶段简化备注（§5 卡牌奖励机制），说明当前使用初始卡组占位，rareOffset 等暂未接入
  - INDEX 版本号 v0.9.0→v0.9.1，文档统计更新（30 完成 / 2 部分 / 12 待设计）

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| CardReward 跳过 | 移除跳过按钮 | 对标尖塔2：关闭 overlay 不选即为跳过；多一个按钮只是冗余 |
| 地图与路径文档 | 填成 v1.0，以 MVP 实际实现为准 | 文档应反映当前代码真实状态，非理想设计 |

---
## 2026-07-06（十六续）| 战斗奖励逻辑重做

### 完成事项
- [x] **战后节点状态更新**：击败敌人后 `CurrentState = 空闲` + `BattleOver = true` + 节点标记为已访问
- [x] **新增 PlayerState 枚举**：`DataDefs.cs` 新增 `PlayerState` 枚举（空闲/战斗中），`GameManager` 新增 `CurrentState` 字段
- [x] **灵韵点击领取**：胜利弹窗中的灵韵改为按钮，点击领取后行消失（不再自动结算）
- [x] **卡牌奖励不销毁弹窗**：`ClaimCardReward` 不再销毁胜利弹窗，CardReward overlay 叠在弹窗上方
  - 选卡 → 卡入牌组 → overlay 关 → 回弹窗
  - 跳过 → 只关 overlay → 回弹窗（可反复打开）
  - 不再自动跳转 MapScene
- [x] **"继续"按钮改为呼出地图**：`ShowMapOverlay()` toggle，点击后按钮不消失
- [x] **UI交互文档同步更新**：场景C/D更新流程描述

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 灵韵领取 | 手动点击领取 | 给玩家主动确认的仪式感；对标物理卡牌游戏"收好战利品" |
| 继续按钮 | 呼出地图 overlay（toggle） | 让玩家自己选下一步走哪个节点，不给默认跳转 |
| PlayerState | 独立枚举字段 | 为后续状态判断（S/L、地图节点可访问性等）预留接口 |

---
## 2026-07-07 | 轻量项目管理体系搭建

### 完成事项
- [x] **建立任务看板**：新增 `任务看板.md`，按 Backlog / Ready / Doing / Review / Done / Blocked 管理任务状态。
- [x] **建立版本计划**：新增 `版本计划.md`，明确 v0.0.1-alpha 到 v0.1.0-demo 的阶段目标、范围和发布流程。
- [x] **建立验收清单**：新增 `验收清单.md`，覆盖基础工程、MVP 主流程、UI 和玩法系统验收项。
- [x] **建立风险记录**：新增 `风险记录.md`，记录文档/代码不同步、UI 资源接入、全局状态耦合、奖赏卡池缺失、美术占位、QA 和发布流程等风险。
- [x] **建立决策记录**：新增 `决策记录.md`，记录轻量部门制、QA/发布暂由总项目经理兼任、MVP 优先等项目级决策。
- [x] **更新文档索引**：`INDEX.md` 更新到 v0.9.2，根目录新增 5 份管理文档。

### 新增文档
| 文档 | 状态 |
|------|:--:|
| `任务看板.md` | ✅ |
| `版本计划.md` | ✅ |
| `验收清单.md` | ✅ |
| `风险记录.md` | ✅ |
| `决策记录.md` | ✅ |

### 设计决策记录

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 项目管理方式 | 轻量部门制 + 总项目经理协调 | 当前是独立游戏 demo，重点是减少混乱而不是建立复杂流程 |
| QA职责 | 暂由总项目经理兼任 | MVP 阶段用验收清单即可支撑，后续再评估是否拆独立 QA 部 |
| 发布管理 | 暂由总项目经理兼任，开发部配合 | 先建立版本计划和发布流程，等可玩包稳定后再细化 |
| 管理文档位置 | `洪荒之后-main/docs` 根目录 | 方便所有部门查阅和 INDEX 统一索引 |

### 本轮任务分派

| 任务 | 接收方 | 产出 |
|------|--------|------|
| P0-001 梳理 MVP 闭环最终验收项 | UI/UX部 / 总项目经理 | 复核并补齐 `验收清单.md` |
| P0-002 核对代码实现与 UI 文档是否一致 | 开发部 / UI/UX部 | 文档要求、当前实现、差异、风险、修正建议 |
| P0-003 补齐卡牌与 UI 美术需求 | 美术部 / UI/UX部 | MVP 美术资源规格、命名规则、优先级 |
| P0-004 确定正式奖赏卡池最小方案 | 策划部 | 巫祝 MVP 奖赏卡池 8~12 张方案 |

### 管理动作

- [x] `任务看板.md` 中 P0-001 ~ P0-004 状态更新为 Doing。
- [x] 已向开发部、策划部、美术部、UI/UX部线程下发对应任务。
- [x] P0-005 可玩包导出流程暂不启动，等待主流程验收结果后再推进。
- [x] `验收清单.md` 新增 MVP 最小验收路径。
- [x] 执行基础工程构建：`dotnet build .\AfterHongHuang.csproj` → 0 警告 / 0 错误。

---
## 2026-07-07 | P0-003 卡牌与 UI 美术需求补齐

### 完成事项
- [x] **审查现有美术资源**：`洪荒之后-art/` 当前仅有 Godot 项目壳和默认 `icon.svg`，未发现可用于 MVP 的标题、角色、敌人、背景或 UI 正式资源。
- [x] **补齐 MVP 美术资源规格**：在 `04-美术音频/04-卡牌与UI美术.md` 中补齐标题背景、巫祝角色、山野妖猴、战斗背景、卡牌框、按钮、TopBar、地图卷轴、胜利弹窗、CardReward overlay 的尺寸、格式、命名、用途、优先级和占位方案。
- [x] **补齐交付规范**：新增 `assets/` 目录建议、统一英文命名规则、PNG/WebP/源文件格式规范、色彩与可读性要求、交付说明模板。
- [x] **复用扩展评估**：明确 UI 九宫格、卡牌稀有度、地图节点、弹窗和 TopBar 的复用方式，并记录卡牌插画量、Godot 导入配置、背景抢读性的主要风险。
- [x] **索引与看板同步**：修正 `04-美术音频` 下卡牌与 UI 美术文档编号不一致问题，任务看板 P0-003 标记为 Done。

### 更新文档
| 文档 | 变更 |
|------|------|
| `04-美术音频/04-卡牌与UI美术.md` | 占位文档扩展为 MVP 美术资源规格 v0.2 |
| `04-美术音频/00-美术音频总览.md` | 子文档索引同步到实际文件名与状态 |
| `INDEX.md` | 美术音频目录树和剩余 P0 任务同步 |
| `任务看板.md` | P0-003 状态更新为 Done，关联文档改为实际路径 |

### 验证结果
- 文档审查完成；本次未修改 `洪荒之后-code/` 和 Godot 工程，无需运行 `dotnet build`。

### 已知问题
- 实际美术资源尚未产出，后续需按 P0 清单交付占位或正式图。
- `洪荒之后-art` 当前 Git 工作树配置无法正常读取状态，未对资源文件做任何删除、重命名或移动。

---
## 2026-07-07 | 开发部 P0 修复回收

### 完成事项
- [x] **P0-006 体修金刚重复加生命修复完成**：开发部移除 `GameManager.InitBattle()` 中的重复加血逻辑，体修金刚只在道痕固化时应用。
- [x] **P0-007 战斗中地图只读状态修复完成**：从 Map 进入战斗和 Battle 初始化时设置 `CurrentState = 战斗中`，胜利后恢复为空闲。
- [x] **P0-008 巫祝正式奖赏卡池接入完成**：`RewardCardPool` 已替换为 `wx_r_001` ~ `wx_r_010`。
- [x] **消弭最小实现完成**：新增 `ExhaustPile`，消弭牌本场战斗移出循环，战斗结束归还。
- [x] **项目管理同步**：`任务看板.md` 中 P0-006 / P0-007 / P0-008 标记 Done，新增 P0-009 MVP 最小验收。
- [x] **风险同步**：`风险记录.md` 中 R-004 标记已解决，R-009 / R-010 标记已解决、待手动验收确认。

### 开发部验证结果
`dotnet build .\AfterHongHuang.csproj` → 0 错误 / 0 警告。

### 下一步
执行 P0-009：按 `验收清单.md` 做 MVP 最小手动验收，重点确认：

- 体修金刚 HP 只从 80 变 90。
- 战斗中地图只能查看，不能进入新节点。
- 胜利后继续呼出的地图可以回到探索节奏。
- 战利品奖励能抽到正式奖赏卡池。
- 消弭牌不会永久丢失。

### 已知问题
- P1-005 未处理：CardReward 跳过按钮、奖励加入抽牌堆还是弃牌堆仍需统一。
- 奖赏卡池仍为简单随机 3 选 1，rareOffset、稀有度权重和升级概率未接入。
- 本次开发部只做 C# 编译验证，未执行 Godot 实机/手动流程验收。

---
## 2026-07-07 | 用户实机 BUG 反馈登记与分派

### 用户反馈
1. **卡牌奖励可重复选择**：战斗胜利后选择卡牌奖励，选完后仍能再次进入奖励选择并继续拿卡。
2. **套牌页面重复叠开**：TopBar 套牌按钮点多次会打开多个页面，关闭时需要点很多次。

### 处理结论
- 暂不新建 QA 部。当前阶段由总项目经理兼任 QA 入口：用户反馈 → 总项目经理登记/定级 → 分派开发部/UI/UX部 → 修复后回归。
- BUG-20260707-001 定为 P0：重复领取奖励会破坏核心战斗奖励循环。
- BUG-20260707-002 定为 P1：重复叠开套牌页面影响 UI 操作体验。

### 已登记任务
| ID | 等级 | 负责人 | 状态 | 验收 |
|----|------|--------|------|------|
| BUG-20260707-001 | P0 | 开发部 / UI/UX部 | Doing | 卡牌奖励只能领取一次；领取后奖励行消失，下方奖励上移 |
| BUG-20260707-002 | P1 | 开发部 / UI/UX部 | Doing | 套牌查看改为 toggle，最多只存在一个页面 |

### 文档更新
- `任务看板.md`：新增两个 BUG 任务。
- `验收清单.md`：新增 MVP-022 和 UI-014 验收项。
- 已向开发部线程下发修复任务。

---
## 2026-07-07 | 用户实机 BUG 修复回收

### 完成事项
- [x] **BUG-20260707-001 卡牌奖励重复领取修复完成**：胜利弹窗奖励列表改为 `VBoxContainer` 排列；卡牌奖励按次数生成独立入口，选完后该行移除，防止重复加卡；符修遗箓额外奖励作为独立行处理。
- [x] **奖励行上移补位完成**：灵韵领取后行移除，下面的奖励项自动上移，不留下明显空洞。
- [x] **BUG-20260707-002 套牌页面重复叠开修复完成**：`DeckViewer` 改为单实例 toggle，重复点击套牌按钮会关闭当前页面；关闭按钮和遮罩使用同一关闭路径。
- [x] **项目管理同步**：`任务看板.md` 中两个 BUG 标记 Done，`验收清单.md` 记录构建通过、待实机回归。

### 开发部验证结果
`dotnet build .\AfterHongHuang.csproj` → 0 错误 / 0 警告。

### 仍需回归
- 实机点击确认卡牌奖励领取后入口消失并不能重复加卡。
- 实机点击确认符修遗箓额外奖励作为独立奖励行出现。
- 实机点击确认套牌按钮一开一关，不叠多个页面。

### 已知问题
- CardReward 的“跳过”仍沿用现状：只关闭 overlay，不领取该奖励行。
- 本次未做美术表现重做，奖励上移和套牌 toggle 先以功能正确为主。

---
## 2026-07-07 | 流程纠偏：BUG 反馈入口与执行边界

### 事件
开发部线程上报：用户在开发部线程反馈 `BUG1：初始道痕节点出门后仍可点击` 后，开发部曾准备直接修改代码，并请求总项目经理确认流程。

### 项目经理决策
- 不新建部门。当前阶段仍由总项目经理兼任 QA 入口。
- 用户反馈 BUG 的标准入口是总项目经理；总项目经理负责登记、定级、分派。
- 开发部是执行线程，收到总项目经理明确分派后再修改代码。
- 如果用户直接在开发部线程明确要求“现在修”，开发部可以执行，但仍需补齐任务记录、开发记录和验证结果。
- 本次问题正式登记为 `BUG-20260707-003：初始道痕节点出门后仍可点击`，并授权开发部修复。

### 已登记任务
| ID | 等级 | 负责人 | 状态 | 验收 |
|----|------|--------|------|------|
| BUG-20260707-003 | P0 | 开发部 | Doing | 离开初始道痕节点后，道韵/道痕起点不可再次点击进入 |

### 当前判断
- 总项目经理检查当前工作区时，`MapRenderer.cs` 仍为“道韵始终可访问”的旧逻辑，未发现需要立即回滚的已落地改动。
- 已向开发部明确：无需回滚修复方向，按正式 BUG 任务完成修复、构建和记录。

---
## 2026-07-07 | BUG-20260707-003 修复回收

### 完成事项
- [x] **初始道痕节点出门后仍可点击修复完成**：开发部将共用地图判定调整为道韵起点只在玩家仍位于初始节点时可交互。
- [x] **地图 overlay 表现同步**：不可访问节点在共用地图渲染中显示灰态，MapScene 与 Battle overlay 表现一致。
- [x] **开发记录同步**：开发部已更新 `洪荒之后-code/docs/开发记录-code.md`。
- [x] **项目管理同步**：`任务看板.md` 中 BUG-20260707-003 标记 Done，`验收清单.md` 记录构建通过、待实机回归。

### 开发部验证结果
`dotnet build .\AfterHongHuang.csproj` → 0 错误 / 0 警告。

### 仍需回归
- 新游戏未出门前，道痕节点仍可用于当前流程。
- 固化道痕并进入第一个战斗/后续节点后，初始道痕节点不可点击。
- 战斗中打开地图 overlay 时，初始道痕节点同样不可点击且为灰态。

---
## 2026-07-07 | 第一轮 P0 审查收口与开发修复分派

### 完成事项
- [x] **收口 P0-001**：UI/UX部已补齐 `验收清单.md`，形成玩家视角 MVP 最小 UI 验收路径。
- [x] **收口 P0-002**：开发部与UI/UX部已完成代码/UI 文档一致性审查，确认当前存在 2 个 P0 阻断项。
- [x] **确认 P0-003/P0-004 已完成**：美术资源规格和巫祝 MVP 奖赏卡池方案均已落文档。
- [x] **新增开发修复任务**：在 `任务看板.md` 新增 P0-006、P0-007、P0-008。
- [x] **新增风险记录**：在 `风险记录.md` 记录体修金刚重复加生命、战斗中地图只读、CardReward 跳过/奖励加入位置不一致等风险。
- [x] **下发开发部修复任务**：已要求开发部直接修复 P0-006 / P0-007，并接入 P0-008。

### 新增任务
| ID | 任务 | 负责人 | 状态 |
|----|------|--------|------|
| P0-006 | 修复体修金刚重复加生命 | 开发部 | Doing |
| P0-007 | 修复战斗中地图只读状态 | 开发部 | Doing |
| P0-008 | 接入巫祝 MVP 正式奖赏卡池 | 开发部 | Ready |
| P1-005 | 统一 CardReward 跳过与奖励加入位置 | 开发部 / UI/UX部 | Backlog |

### 当前判断
- 暂无需要用户决策的事项。
- CardReward 跳过按钮和奖励加入位置属于 P1 体验一致性问题，不阻塞当前主流程修复。
- 可玩包导出流程继续暂缓，等 P0-006 / P0-007 修复并完成最小手动验收后再启动。

---
## 2026-07-07 | P0-004 巫祝 MVP 奖赏卡池最小方案

### 完成事项
- [x] **确定巫祝正式最小奖赏卡池**：10 张卡，黄品 6 / 玄品 3 / 地品 1，替代"奖励池暂用初始卡"占位方案。
- [x] **保持 MVP 机制边界**：只使用伤害、护体、斗劲、易损、永炎、自损、消弭；不新增关键词，不修改奖励概率机制。
- [x] **补齐卡牌设计字段**：每张卡记录名称、类型、费用、效果、设计目的、风险备注。
- [x] **完成数值与复用性审查**：标注山野妖猴、九乌坠日、体修金刚、符修遗箓等当前 MVP 内容的兼容风险。
- [x] **项目管理同步**：P0-004 标记 Done，R-004 从"未完成"调整为"方案完成，待代码接入"。

### 新增/更新文档
| 文档 | 变更 |
|------|------|
| `03-玩法系统/01-卡牌系统.md` | v0.9.0→v1.0.0，新增 §10 巫祝 MVP 奖赏卡池（正式最小方案） |
| `MVP开发需求-第一轮.md` | v2.0→v2.1，卡牌奖励数据改为引用正式最小池 |
| `06-UI交互/01-MVP界面交互.md` | 卡牌奖励说明替换为正式最小池，标注代码待接入 |
| `02-角色/01-巫祝.md` | v0.7.0→v0.8.0，下一步改为扩展升级版、天品池、低血量/涅槃机制 |
| `INDEX.md` | v0.9.2→v0.9.3，P0 卡牌系统状态更新 |
| `任务看板.md` | P0-004 状态 Doing→Done，并加入 Done 列表 |
| `风险记录.md` | R-004 改为正式池待代码接入 |
| `决策记录.md` | 新增 ADR-005：巫祝 MVP 奖赏卡池先用 10 张小池 |

### 设计决策记录
| 决策项 | 选择 | 理由 |
|--------|------|------|
| 池规模 | 10 张 | 满足 8~12 张要求，足以替代初始卡占位，同时控制开发和测试量 |
| 品级结构 | 黄品6 / 玄品3 / 地品1 / 天品0 | 贴合常规奖励概率；天品保留给 Boss/事件/后续版本 |
| 机制范围 | 不新增关键词、不改概率 | 避免 P0 阶段扩大实现面，优先用已有状态做可玩闭环 |
| 地品方向 | 祝融残焰：永炎 + 自损 + 消弭 | 提供稀有路线差异，并用消弭控制永炎无限叠加风险 |

### 验证结果
- 本次仅修改 `洪荒之后-main/docs` 内文档，未修改 Godot 工程和 C# 代码。
- 已用文本检索确认旧的"奖赏卡池待设计 / 暂用初始卡占位"核心描述已替换为正式最小池或"待代码接入"说明。
- 未运行 `dotnet build .\AfterHongHuang.csproj`，因为本次没有代码或工程文件变更。

### 已知问题
- 当前代码可能仍从拳袭、格挡、烈拳、燃血中抽奖励卡，需要开发部后续接入 `wx_r_001` ~ `wx_r_010`。
- `祝融残焰` 依赖"消弭"可用；若当前战斗逻辑尚未支持，开发接入时需先实现消弭或临时下调永炎层数。
- rareOffset、精英加成、升级概率仍未接入 MVP 代码，后续按卡牌奖励机制文档逐步实现。

---
## 2026-07-07 | P0-009 MVP 最小验收启动

### 完成事项
- [x] **确认下一步**：P0 代码修复与 BUG 修复均已收口，当前应进入 MVP 最小验收，而不是继续新增功能。
- [x] **更新任务状态**：`任务看板.md` 中 P0-009 已从 Ready 调整为 Doing。
- [x] **明确验收范围**：按 `验收清单.md` 的 MVP 最小 UI 验收路径执行，重点覆盖启动、角色选择、道痕固化、地图进入战斗、战斗胜利、奖励领取、继续回地图。

### 下一步
- UI/UX部执行玩家视角手动验收，记录通过项、失败项、复现步骤和归属建议。
- 总项目经理读取验收结果后更新 `任务看板.md`、`验收清单.md`、`风险记录.md`，并将失败项分派给对应部门。

### 验证结果
- 本次仅更新项目管理文档，未修改 Godot 工程和 C# 代码。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---
## 2026-07-07 | P0-009 UI/UX 验收回收

### 完成事项
- [x] **读取UI/UX部验收结果**：确认 P0-009 已完成 headless 场景交互验收并更新 `验收清单.md`。
- [x] **确认通过范围**：Title、CharacterSelect、Map 道痕底页、未固化锁图、固化后进入 Battle、套牌 toggle、战斗中地图只读、胜利弹窗、灵韵领取、CardReward overlay、奖励不可重复领取、胜利后继续探索和初始道痕节点禁用均通过。
- [x] **确认未覆盖范围**：步骤 10~13（斗击/术法真实点击出牌、灵力不足灰态、结束回合与敌人行动）未被 headless 稳定覆盖，不能伪造成实机通过。
- [x] **任务状态同步**：`任务看板.md` 中 P0-009 从 Doing 调整为 Review，新增 P0-010 窗口实机补测任务。
- [x] **风险同步**：`风险记录.md` 新增 R-012（缺窗口实机补测）和 R-013（headless 退出期地图收卷异步日志）。

### 验收结论
- P0-009 暂不进入 Done，状态为 Review。
- 当前没有明确产品失败项，不向开发部下发修复任务。
- P0-005 可玩包导出流程暂不启动；等待 P0-010 补测通过后再推进。

### UI/UX部验证结果
- `dotnet build .\AfterHongHuang.csproj` → 0 错误 / 0 警告。
- Godot 4.7 .NET console headless 可启动，`GameManager` Autoload 正常初始化。
- headless 场景交互：18 通过 / 0 失败 / 1 无法执行。

### 下一步
- UI/UX部执行 P0-010：用可见窗口实机补测步骤 10~13。
- 若 P0-010 通过，总项目经理将 P0-009 / P0-010 收口，并启动 P0-005 可玩包导出流程。

---
## 2026-07-07 | 战斗 UI 近期目标登记与分派

### 背景
用户提出三项近期开发目标：

- 战斗日志占视野并可能遮挡手牌，需要保留调试能力，但正式体验不应影响出牌。
- 胜利奖励阶段删除未开放的“道器碎片”占位。
- 战斗手牌交互向杀戮尖塔式体验靠拢：底部弧形手牌、点击选中隆起、攻击牌箭头、长按拖拽到目标释放出牌。

### 完成事项
- [x] **方案选择**：战斗日志采用“默认精简/收起 + 日志开关按钮 + 展开不覆盖手牌”的方案，兼顾 demo 调试和正式体验。
- [x] **任务登记**：`任务看板.md` 新增 BUG-20260707-004、P0-011、P0-012。
- [x] **验收同步**：`验收清单.md` 更新战斗步骤 10/11/14，并新增 UI-015、UI-016、UI-017。
- [x] **风险同步**：`风险记录.md` 新增 R-014、R-015。
- [x] **调整验收节奏**：P0-010 暂停，等待 P0-012 完成后按新版手牌交互重新窗口实机补测。

### 新增任务
| ID | 任务 | 负责人 | 状态 |
|----|------|--------|------|
| BUG-20260707-004 | 战斗日志遮挡手牌 | 开发部 / UI/UX部 | Ready |
| P0-011 | 移除胜利奖励道器碎片占位 | 开发部 | Ready |
| P0-012 | 战斗手牌弧形布局与点击/拖拽出牌 | UI/UX部 / 开发部 | Ready |

### 验证结果
- 本次仅更新项目管理与验收文档，未修改 Godot 工程和 C# 代码。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---
## 2026-07-07 | UI/UX部 P0-012 交互口径补齐

### 完成事项
- [x] **补齐战斗日志交互口径**：在 `06-UI交互/01-MVP界面交互.md` 中明确日志按钮、默认收起、展开、收起三种状态；日志用于 demo 调试，但默认正式体验不显示大日志。
- [x] **明确日志鼠标边界**：日志默认不得遮挡手牌、灵力、结束回合按钮；展开时面板高度受限，不得覆盖手牌操作热区。
- [x] **补齐新版手牌交互验收口径**：明确底部轻弧形排布、hover/点击隆起、攻击牌箭头、自身术法目标反馈、长按拖拽到合法目标释放出牌、非法释放回弹取消。
- [x] **同步胜利弹窗奖励口径**：MVP 胜利弹窗只显示可用奖励和继续入口，不显示“道器碎片 ×1（暂未开放）”等未开放占位。
- [x] **记录代码冲突点**：在 UI 文档新增 P0-012 当前代码冲突记录，标明日志开关、手牌弧形、拖拽释放、胜利奖励占位需要开发部/UI/UX 实现支持。

### 修改文档
| 文档 | 变更 |
|------|------|
| `06-UI交互/01-MVP界面交互.md` | v2.0.1 → v2.1.0，补齐日志按钮、手牌点击/拖拽、胜利奖励内容和当前代码冲突表 |

### 归属建议
- BUG-20260707-004：开发部实现日志开关与默认收起，UI/UX部验收遮挡/鼠标拦截问题。
- P0-011：开发部删除胜利弹窗道器碎片占位，UI/UX部验收奖励内容。
- P0-012：UI/UX部可提供 UI 实现支持，开发部负责接入现有战斗逻辑；完成后由UI/UX部执行窗口实机验收。

### 验证结果
- 本次仅修改 UI 交互文档，未修改 `洪荒之后-code/`、Godot 场景或工程配置。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---
## 2026-07-07 | P0-012 手牌交互验收反馈与返工

### 用户反馈
- 当前手牌排列像散乱旋转，不是参考图中的底部牌扇。
- hover 应表现为原卡卡面放大展示，而不是只轻微上浮或布局错位。
- 之前“箭头从角色指出”的文档表述错误，应改为从卡牌指出箭头。
- 攻击牌本体不应飞出手牌栏，出手牌区的主要视觉反馈应是箭头；自身术法卡可在点击选中或长按拖动时离开手牌区。
- 参考截图来自杀戮尖塔式手牌：以屏幕下方虚拟圆心形成扇形，中心卡最高最正，左右卡逐步降低并轻旋。

### 完成事项
- [x] **纠正 UI 文档**：`06-UI交互/01-MVP界面交互.md` 已将箭头起点改为卡牌，并补充牌扇算法、hover 放大、攻击牌不飞出手牌栏等约束。
- [x] **更新验收口径**：`验收清单.md` 步骤 10/11、UI-010、UI-011、UI-016、UI-018 已更新。
- [x] **任务打回返工**：`任务看板.md` 中 P0-012 从 Review 调整为 Doing。
- [x] **风险记录**：`风险记录.md` 新增 R-016，并将 R-015 重新调整为监控中。

### 下一步
- 开发部按新口径返工 `BattleController.cs` 的手牌布局、hover、选中、箭头起点和拖拽边界。
- UI/UX部按用户截图复核手牌视觉和实机手感。
- P0-010 继续等待 P0-012 返工完成后再做窗口实机补测。

### 验证结果
- 本次仅更新项目管理与 UI 验收文档，未修改 Godot 工程和 C# 代码。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---
## 2026-07-08 | UI/UX部 P0-012 新口径复核

### 完成事项
- [x] 复核用户提供的当前错误实现截图与杀戮尖塔式参考截图。
- [x] 确认新口径清晰：手牌必须是底部虚拟圆心牌扇，hover 原位放大，斗击箭头从卡牌指出，攻击牌本体不得飞出手牌栏，自身术法卡允许作为例外拖出到合法自身区域。
- [x] 在 `06-UI交互/01-MVP界面交互.md` 补充 “P0-012 返工验收建议”，用于开发部自测与UI/UX部窗口实机补测。

### 验证结果
- 本次仅更新 UI 文档与项目开发记录，未修改 `洪荒之后-code/`、Godot 场景或 C# 代码。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---
## 2026-07-09 | 项目级 Agent 工作流升级

### 背景
用户提供了一组面向总 agent / 分 agent 的提示词，希望强化项目级记忆、角色边界、任务卡机制、工程质量和部门启动检查。

### 完成事项
- [x] **强化项目经理红线**：在 `AGENTS.md` 的“角色与权限边界”开头新增核心前提，明确项目负责人/项目经理/总项目经理不是实现者，默认不直接修改代码、资源或具体实现文件。
- [x] **升级根目录 `AGENTS.md`**：新增 QA / 质量保证、构建发布、音频、本地化 / 发行运营等支持角色说明。
- [x] **补充工程质量红线**：明确新 UI 优先使用场景、主题、资源和可复用控件；现有动态 UI 仅允许 P0 小范围修补，并需记录技术债。
- [x] **补充兜底规则**：禁止静默兜底、假成功、空 catch 和无提示 return；允许边界保护，但必须留下可追踪信息。
- [x] **补充注释规则**：公共 API、复杂状态机、跨模块流程、坐标/动画算法、异步流程必须注释；简单代码不强制垃圾注释。
- [x] **建立部门任务启动与回报协议**：每个部门任务开始前检查 `AGENTS.md`、工作区、任务卡、相关文档；完成后按固定格式回报验证、文档和风险。
- [x] **明确并线工作规则**：总项目经理可分发部门线程并线工作，但必须说明分工、回收结论、冲突处理和残留风险。
- [x] **创建支持部门线程**：新增 QA质量保证部、构建发布部、音频部、本地化发行运营部，均已写入初始职责边界和启动检查要求。

### 新增部门线程
| 部门 | 线程 ID | 主要职责 | 默认边界 |
|------|---------|----------|----------|
| QA质量保证部 | `019f460c-ab5b-7cf3-822b-c93a833f5e18` | 独立审查、找茬、验收、复现、风险发现 | 不直接修代码、不改资源、不伪造通过 |
| 构建发布部 | `019f460c-b463-7841-b2f4-f09122c7deec` | 构建、导出、版本号、发布说明、可玩包验证 | 不修玩法/UI BUG，不发布未经最小验收的包 |
| 音频部 | `019f460c-bebf-7401-b6bd-4e5eb3273fd3` | BGM、UI音效、战斗音效、音频资源规格与文档 | 不直接接代码、不删除或重命名资源 |
| 本地化发行运营部 | `019f460c-c62a-7801-9178-c8ef61544a2d` | 本地化文本、版本公告、商店页草案、反馈整理 | 不改代码、不承诺日期/平台/价格 |

### 判断
- QA / 找茬 agent 有价值，应作为独立审查职责使用，尤其适合 P0 验收、UI 手感、回归测试和发布前检查。
- 构建发布、音频、本地化 / 发行运营当前可先作为“角色”存在，不急于新建常驻线程；当对应任务进入 P0/P1 生产，再单独建部门线程更合适。

### 验证结果
- 本次仅修改项目级流程文档和管理开发记录，未修改 Godot 工程和 C# 代码。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---
## 待续...

---
## 2026-07-16 | P0 交互语义统一整改登记

### 用户窗口反馈
- CardReward 的“跳过”关闭后，胜利页卡牌奖励行被永久删除。
- 地图后续节点超出可视区，缺少滚轮或拖拽平移；当前动画仍是从右侧滑入的抽屉，不符合后续卷轴美术替换方向。
- 灵脉、商店、事件等节点页仍可能在“继续/返回地图”后清空当前页面并显示无价值底页。

### 代码侧确认
- `BattleController.AddCardRewardRow()` 的取消回调调用 `TrySkipBattleCardReward()`，成功后 `QueueFree()` 奖励入口，属于超出按钮字面职责的隐藏副作用。
- 候选牌由 `RewardResolver` 在胜利计划建立时使用稳定 Reward 随机流生成，并保存在 `RewardPlan.Candidates`；CardReward overlay 本身不应重抽。
- `MapController` 和 `BattleController` 各自实现了右侧滑入地图动画；`MapRenderer` 直接把全部图层绘制到固定容器，没有横向视口。
- `LingmaiController`、`ShopController`、`EventController` 仍有成功或返回后直接 `GoToScene("res://scenes/Map/Map.tscn")` 的路径。

### 冻结验收口径
- CardReward 跳过只关闭 overlay，奖励行保留，候选顺序和已选进度不变。
- 地图支持滚轮横移、左键拖拽、边界限制和拖拽/节点点击隔离；卷轴从左向右揭示。
- 所有节点页继续/返回地图只打开 overlay；当前页和结果保留；已使用操作按钮灰显；仅点击下一节点离场。
- QA 全量审查所有按钮回调、场景切换、页面清理和奖励/节点状态副作用；任何空白占位页或隐藏副作用按 P0 失败。

### 验证结果
- 本次仅更新项目管理、验收和风险文档，未修改 `洪荒之后-code/`。
- 未运行 `dotnet build .\AfterHongHuang.csproj`。

---

## 2026-07-17 | UI-FOCUS-SETTINGS 自动化收口

### 完成事项
- [x] 胜利奖励总面板调整为 TopBar 下缘以下的内容区模态：暗化 alpha `0.72`，手牌、日志、结束回合、Debug 和 F9 不可穿透；TopBar 仍是全局常驻可交互入口。
- [x] CardReward、地图、工具 overlay 层级收敛为集中常量：胜利 `400`、地图/工具 `450`、CardReward `470`。
- [x] CardReward 打开时通过 TopBar 打开地图、套牌或设置会走可恢复取消链；关闭全局 overlay 后奖励行、候选和已选进度保持，且不消费奖励、不提交节点、不离场。
- [x] 地图卷轴终态统一为 `Rect2(64,104,1792,872)`，支持左起右展、滚轮、左键拖拽与边界限制；灵脉返回地图不再隐式提交结果。
- [x] 修复保存分辨率读取时 Godot `Variant` 直接转换导致的启动异常；设置具备真实应用、持久化、非法配置拒绝与失败回滚路径。

### 验收结论
- QA C3 严格只读自动化通过：Debug/Release build、Godot 4.7 Mono headless、500 seeds、Battle/Map/Lingmai/Shop/Event 场景加载和硬编码门禁均通过。
- 任务进入 `Review`，不提前标记 Done。用户仍需窗口验证 TopBar 与胜利内容区的输入边界、CardReward 经 TopBar overlay 后的恢复、地图拖拽滚轮及 1920x1080/1280x720 分辨率行为。
- P1 保留 R-039；P2 为设置菜单文档与真实窗口设置实现的同步，已下发 UI/UX部。
